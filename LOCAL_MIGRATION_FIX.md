# 🔧 Исправление: Применение миграций локально

## Проблема

При попытке применить миграции через `railway run` возникает ошибка:
```
Error: P1001: Can't reach database server at `postgres.railway.internal:5432`
```

Это происходит потому, что `railway run` запускает команду внутри Railway контейнера, где `DATABASE_URL` указывает на внутренний адрес `postgres.railway.internal`, который недоступен локально.

## ✅ РЕШЕНИЕ: Использовать публичный DATABASE_URL локально

### Шаг 1: Получите публичный DATABASE_URL

У вас уже есть публичный доступ к PostgreSQL:
- **Публичный домен:** `postgres-production-619b.up.railway.app:5432`
- **TCP Proxy:** `shortline.proxy.rlwy.net:24524`

### Шаг 2: Получите пароль

1. Откройте Railway → PostgreSQL сервис → **Variables**
2. Найдите `POSTGRES_PASSWORD`
3. Скопируйте значение

### Шаг 3: Примените миграции ЛОКАЛЬНО (без railway run)

```bash
cd /Users/symonbaikov/make-bot/backend

# Установите DATABASE_URL с публичным хостом
# ЗАМЕНИТЕ PASSWORD на реальный пароль из Railway Variables
export DATABASE_URL="postgresql://postgres:PASSWORD@postgres-production-619b.up.railway.app:5432/railway"

# Проверьте подключение
npx prisma db execute --stdin <<< "SELECT 1;"

# Если подключение работает, примените миграции
npx prisma db push --skip-generate --accept-data-loss

# Создайте админа
npm run db:seed
```

### Альтернатива: Использовать TCP Proxy

Если публичный домен не работает:

```bash
export DATABASE_URL="postgresql://postgres:PASSWORD@shortline.proxy.rlwy.net:24524/railway"
npx prisma db push --skip-generate --accept-data-loss
npm run db:seed
```

## Почему railway run не работает?

`railway run` запускает команду внутри Railway контейнера, где:
- `DATABASE_URL` указывает на `postgres.railway.internal` (внутренний адрес)
- Этот адрес доступен только внутри Railway сети
- Локально вы не можете подключиться к внутреннему адресу

**Решение:** Используйте публичный `DATABASE_URL` локально, не через `railway run`.

## Проверка после применения

1. **Railway → PostgreSQL → Database → Data**
   - Должны быть таблицы: `web_users`, `users`, `sessions`, `actions`

2. **Попробуйте залогиниться:**
   - `https://your-backend.railway.app/`
   - Email: `admin@example.com`
   - Password: `admin123`

3. **Проверьте логи Backend:**
   - Не должно быть ошибок про `web_users does not exist`

