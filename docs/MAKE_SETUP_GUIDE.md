# Make Setup Guide - Пошаговая инструкция

## 🎯 Цель

Настроить автоматизацию в Make для обработки webhook'ов от Backend API и автоматического предоставления доступа пользователям.

---

## 📋 Предварительные требования

1. Аккаунт в Make (https://www.make.com)
2. Доступ к PostgreSQL базе данных
3. Backend API должен быть запущен и доступен
4. Webhook URL'ы из Backend API

---

## 🔧 Шаг 1: Подключение к PostgreSQL

### 1.1 Создание подключения

1. Войдите в Make
2. Перейдите в **Connections** → **Add a connection**
3. Найдите и выберите **PostgreSQL**
4. Заполните форму:

```
Connection name: Make Bot PostgreSQL
Host: your-database-host.com (или localhost для разработки)
Port: 5432
Database: make_bot
Username: makebot
Password: makebot123
SSL Mode: require (для production) или disable (для development)
```

5. Нажмите **Save** и протестируйте подключение

---

## 📥 Шаг 2: Создание Bot Webhook Scenario

### 2.1 Создание нового сценария

1. Нажмите **Create a new scenario**
2. Назовите сценарий: `Bot Email Collection`

### 2.2 Добавление Webhook модуля

1. Добавьте модуль **Webhooks** → **Custom webhook**
2. Настройки:
   - **Webhook name:** `Bot Email Collection`
   - **Method:** `POST`
   - Скопируйте **Webhook URL** - это нужно добавить в Backend `.env`:
     ```
     MAKE_WEBHOOK_URL_BOT=<скопированный-url>
     ```

### 2.3 Добавление PostgreSQL модуля - Upsert Session

1. Добавьте модуль **PostgreSQL** → **Execute a query**
2. Выберите подключение: `Make Bot PostgreSQL`
3. Вставьте SQL запрос из `MAKE_INTEGRATION.md` (Step 2 Scenario 1)
4. Настройте маппинг полей из webhook

### 2.4 Добавление PostgreSQL модуля - Upsert User

1. Добавьте модуль **PostgreSQL** → **Execute a query**
2. Вставьте SQL запрос для upsert пользователя
3. Настройте маппинг полей

### 2.5 Добавление PostgreSQL модуля - Log Action

1. Добавьте модуль **PostgreSQL** → **Execute a query**
2. Вставьте SQL запрос для логирования действия
3. Настройте маппинг полей

### 2.6 Добавление Router для проверки платежа

1. Добавьте модуль **Flow control** → **Router**
2. Настройте условие:
   - Route 1: Если статус = `PAID` или `COMPLETED`
   - Route 2: Иначе

### 2.7 Добавление HTTP модуля для Provisioning (Route 1)

1. Добавьте модуль **HTTP** → **Make a request**
2. URL: Webhook URL для Provisioning сценария
3. Method: `POST`
4. Body: JSON с данными для provisioning

### 2.8 Сохранение и активация

1. Нажмите **Save**
2. Нажмите **Run once** для тестирования
3. Если все работает, нажмите **Turn on** для активации

---

## 💳 Шаг 3: Создание PayPal Webhook Scenario

### 3.1 Создание нового сценария

1. Создайте новый сценарий: `PayPal Payment Handler`

### 3.2 Добавление Webhook модуля

1. Добавьте **Webhooks** → **Custom webhook**
2. Скопируйте Webhook URL и добавьте в Backend `.env`:
   ```
   MAKE_WEBHOOK_URL_PAYPAL=<скопированный-url>
   ```

### 3.3 Добавление проверки дубликатов

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для проверки существующего `txn_id`

### 3.4 Добавление Router

1. Проверка на дубликаты
2. Route 1: Дубликат найден → завершить
3. Route 2: Новый платеж → продолжить

### 3.5 Обновление сессии

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для обновления сессии с данными платежа

### 3.6 Логирование

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для логирования действия `PAYMENT_RECEIVED`

### 3.7 Проверка email и Provisioning

1. Добавьте Router для проверки наличия email
2. Если email есть → вызвать Provisioning webhook

---

## ✅ Шаг 4: Создание Access Provisioning Scenario

### 4.1 Создание нового сценария

1. Создайте сценарий: `Access Provisioning`

### 4.2 Webhook Trigger

1. Добавьте **Webhooks** → **Custom webhook**
2. Скопируйте URL для использования в других сценариях

### 4.3 Получение данных сессии

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для получения данных сессии

### 4.4 Определение Final Email

1. Добавьте модуль **Set variables**
2. Логика: `email_user` если существует, иначе `email_paypal`

### 4.5 Расчет End Date

1. Добавьте модуль **Tools** → **Date/Time formatter**
2. Input: `payment_date`
3. Operation: Add 60 days
4. Output: `end_date`

### 4.6 Отправка Email

1. Добавьте модуль **Email** → **Send an email**
2. Настройте SMTP подключение
3. Заполните:
   - To: `{{finalEmail}}`
   - Subject: `Access Granted`
   - Body: HTML или текст шаблон

### 4.7 Обновление статуса

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для обновления статуса на `COMPLETED`
3. Установка `end_date`

### 4.8 Логирование

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для логирования `ACCESS_GRANTED`

### 4.9 Google Sheets Sync (Optional)

1. Добавьте модуль **Google Sheets** → **Add a row**
2. Настройте подключение к Google Sheets
3. Маппинг полей

---

## 🔔 Шаг 5: Создание Nightly Checks Scenario

### 5.1 Создание нового сценария

1. Создайте сценарий: `Nightly Checks`

### 5.2 Schedule Trigger

1. Добавьте модуль **Schedule** → **Schedule**
2. Настройки:
   - **Frequency:** Daily
   - **Time:** `02:00`
   - **Timezone:** Ваш часовой пояс

### 5.3 Поиск pending записей

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для поиска записей со статусом `PAID_PENDING_EMAIL` старше 24 часов

### 5.4 Router

1. Проверка наличия записей
2. Route 1: Записи найдены → отправить алерт
3. Route 2: Записей нет → завершить

### 5.5 Telegram Alert

1. Добавьте модуль **Telegram** → **Send a message**
2. Настройте подключение к Telegram Bot
3. Chat ID: ID администратора
4. Сообщение с информацией о pending записях

### 5.6 Логирование

1. Добавьте **PostgreSQL** → **Execute a query**
2. SQL для логирования алерта

---

## 🔐 Шаг 6: Настройка переменных окружения в Make

1. Перейдите в **Settings** → **Variables**
2. Добавьте следующие переменные:

```
ADMIN_TELEGRAM_CHAT_ID=your-telegram-chat-id
ADMIN_PANEL_URL=https://your-admin-panel-url.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

---

## 🧪 Шаг 7: Тестирование

### 7.1 Тест Bot Webhook

1. Откройте сценарий "Bot Email Collection"
2. Нажмите **Run once**
3. Отправьте тестовый webhook из Backend или используя curl
4. Проверьте выполнение каждого модуля
5. Проверьте данные в PostgreSQL

### 7.2 Тест PayPal Webhook

1. Откройте сценарий "PayPal Payment Handler"
2. Нажмите **Run once**
3. Отправьте тестовый webhook
4. Проверьте обновление сессии
5. Проверьте логирование

### 7.3 Тест Provisioning

1. Откройте сценарий "Access Provisioning"
2. Нажмите **Run once**
3. Отправьте тестовый webhook
4. Проверьте отправку email
5. Проверьте обновление статуса

---

## ✅ Checklist

- [ ] Подключение к PostgreSQL создано и протестировано
- [ ] Scenario 1: Bot Email Collection создан и активирован
- [ ] Scenario 2: PayPal Payment Handler создан и активирован
- [ ] Scenario 3: Access Provisioning создан и активирован
- [ ] Scenario 4: Nightly Checks создан и активирован
- [ ] Все webhook URL добавлены в Backend `.env`
- [ ] Переменные окружения настроены в Make
- [ ] SMTP подключение настроено
- [ ] Telegram бот настроен для алертов (опционально)
- [ ] Все сценарии протестированы
- [ ] Полный flow протестирован end-to-end

---

## 🚀 Активация

После завершения настройки:

1. Убедитесь, что все сценарии включены (Turn on)
2. Обновите Backend `.env` с webhook URL'ами
3. Перезапустите Backend API
4. Протестируйте полный flow:
   - Telegram bot → Email → Backend → Make → Database
   - PayPal payment → Backend → Make → Provisioning → Email

---

## 📞 Troubleshooting

### Проблема: Webhook не получает данные

**Решение:**

- Проверьте, что webhook URL правильный в Backend `.env`
- Проверьте логи Backend API
- Используйте "Run once" в Make для тестирования

### Проблема: PostgreSQL ошибки

**Решение:**

- Проверьте подключение к базе данных
- Убедитесь, что используются правильные типы данных
- Проверьте синтаксис SQL запросов

### Проблема: Email не отправляется

**Решение:**

- Проверьте SMTP настройки
- Убедитесь, что используется App Password для Gmail
- Проверьте логи в Make

---

## 📚 Дополнительные ресурсы

- [Make Documentation](https://www.make.com/en/help)
- [PostgreSQL Module in Make](https://www.make.com/en/help/apps/postgresql)
- [Webhooks in Make](https://www.make.com/en/help/webhooks)
