# Admin Panel - Веб-интерфейс для управления

## ✅ Планируется в Phase 4: Frontend Development

Веб-интерфейс (Admin Panel) для управления пользователями, платежами и сессиями.

---

## 🎯 Основные функции

### 1. Аутентификация и авторизация

- ✅ Страница логина (`/login`)
  - Email + Password
  - Валидация через React Hook Form + Zod
  - JWT токен для сессии
- ✅ Защищенные маршруты
- ✅ Автоматический logout при истечении токена
- ✅ Role-based access control (ADMIN/MANAGER)

### 2. Dashboard (Главная страница)

- ✅ Карточки с ключевыми метриками
  - Общее количество сессий
  - Конверсия (start → email → payment)
  - Выручка по планам
  - Активные платежи
- ✅ Графики:
  - Conversion funnel (воронка конверсии)
  - Revenue by plan (выручка по планам)
  - Trends over time (тренды во времени)
- ✅ Фильтры по датам

### 3. Управление платежами/сессиями

#### Список платежей (`/payments`)

- ✅ Таблица с данными:
  - ID, Session ID, Email, Plan, Amount, Status, Date
- ✅ Фильтры:
  - По статусу (dropdown)
  - По плану (dropdown)
  - По дате (date picker)
  - По email (search input)
- ✅ Поиск:
  - По session_id
  - По txn_id
  - По email
- ✅ Пагинация
- ✅ Сортировка по колонкам
- ✅ Экспорт данных (CSV/Excel)

#### Детали платежа (`/payments/:id`)

- ✅ Отображение всех полей записи
- ✅ Показ обоих email:
  - `email_user` (из Telegram бота)
  - `email_paypal` (из PayPal)
  - `finalEmail` (приоритетный email)
- ✅ Отображение meta данных (JSON viewer)
- ✅ История действий (actions log)
- ✅ Действия:
  - **Resend Email** - повторная отправка email
  - **Grant Access** - ручное предоставление доступа
  - **Edit Email** - редактирование email адреса

### 4. Activity Log (Журнал действий)

- ✅ Страница логов (`/actions`)
- ✅ Таблица с действиями:
  - Тип действия
  - Референс (session_id, txn_id, user_id)
  - Payload (данные события)
  - Дата и время
- ✅ Фильтры:
  - По типу действия
  - По дате
- ✅ Поиск по ref
- ✅ Отображение payload (expandable rows)

### 5. Ручное создание сессии

- ✅ Страница создания (`/sessions/create`)
- ✅ Форма с полями:
  - Plan (BASIC/STANDARD/PREMIUM)
  - Amount (сумма)
  - Currency (валюта)
- ✅ Генерация session_id
- ✅ Создание записи через API
- ✅ Отображение созданной ссылки на бота:
  ```
  https://t.me/your_bot?start=<session_id>
  ```

### 6. Экспорт данных

- ✅ Экспорт в CSV
- ✅ Экспорт в Excel
- ✅ Фильтрация перед экспортом

---

## 🛠 Технологии

### Frontend Stack

- **Framework:** React 18+ или Next.js 14+
- **State Management:** React Query (TanStack Query) для server state
- **UI Library:** Material-UI (MUI) или Ant Design или Tailwind CSS
- **Forms:** React Hook Form + Zod
- **Charts:** Recharts или Chart.js
- **HTTP Client:** Axios
- **Routing:** React Router или Next.js Router

---

## 📊 Структура страниц

```
/admin
├── /login              # Страница входа
├── /dashboard          # Главная страница с метриками
├── /payments           # Список платежей/сессий
├── /payments/:id       # Детали платежа
├── /actions            # Журнал действий
├── /sessions/create    # Ручное создание сессии
└── /export             # Экспорт данных
```

---

## 🔐 Безопасность

- ✅ JWT аутентификация
- ✅ Защищенные маршруты (Protected Routes)
- ✅ Role-based access control
- ✅ Валидация всех входных данных
- ✅ HTTPS в production
- ✅ CORS настройки

---

## 📱 Responsive Design

- ✅ Адаптивный дизайн (mobile-friendly)
- ✅ Оптимизация для планшетов и мобильных устройств
- ✅ Удобная навигация на всех устройствах

---

## 🎨 UI/UX Features

- ✅ Loading states (состояния загрузки)
- ✅ Error states (состояния ошибок)
- ✅ Empty states (пустые состояния)
- ✅ Toast уведомления (успех/ошибка)
- ✅ Модальные окна для подтверждения действий
- ✅ Inline редактирование
- ✅ Фильтры и поиск в реальном времени

---

## 📈 Метрики и аналитика

### Dashboard метрики:

1. **Conversion Funnel:**
   - Started → Awaiting Payment → Paid → Completed
   - Процент конверсии на каждом этапе

2. **Revenue by Plan:**
   - Выручка по каждому плану (BASIC/STANDARD/PREMIUM)
   - Количество продаж по планам

3. **Trends:**
   - График продаж во времени
   - Тренды конверсии
   - Выручка по дням/неделям/месяцам

---

## 🔄 Интеграция с Backend API

Все страницы используют Backend API endpoints:

- `POST /api/admin/auth/login` - Вход
- `GET /api/admin/payments` - Список платежей
- `GET /api/admin/payments/:id` - Детали платежа
- `POST /api/admin/payments/:id/resend` - Повторная отправка email
- `PUT /api/admin/payments/:id/email` - Обновление email
- `POST /api/admin/payments/:id/grant-access` - Предоставление доступа
- `GET /api/admin/stats` - Статистика
- `GET /api/admin/actions` - Журнал действий
- `POST /api/admin/sessions` - Создание сессии
- `GET /api/admin/export` - Экспорт данных

---

## ✅ Статус реализации

**Текущий статус:** Phase 4 еще не начата

**Backend API готов:** ✅

- Все необходимые endpoints реализованы
- Аутентификация настроена
- Валидация работает

**Frontend:** ⏳ Ожидает реализации

---

## 🚀 Следующие шаги

После завершения Phase 3 (Telegram Bot), можно переходить к Phase 4:

1. Инициализация React/Next.js проекта
2. Настройка UI библиотеки
3. Реализация аутентификации
4. Создание Dashboard
5. Реализация страниц управления
6. Добавление графиков и метрик
7. Тестирование и оптимизация

---

## 📝 Примечания

- Админ-панель предназначена для администраторов системы
- Все действия логируются в actions table
- Поддерживается экспорт данных для анализа
- Интерфейс интуитивный и простой в использовании
