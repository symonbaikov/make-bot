# 🔧 Troubleshooting Bot Docker Build

## Ошибка: `Function.executeUserEntryPoint [as runMain]`

Если вы получаете ошибку:
```
at Function.executeUserEntryPoint [as runMain] (node:internal/modules/run_main:128:12)
```

Это означает, что Node.js не может найти или запустить точку входа приложения (`dist/index.js`).

## Решения

### Решение 1: Проверка сборки TypeScript

Убедитесь, что TypeScript компиляция проходит успешно:

1. Проверьте логи сборки в Railway
2. Найдите строку `npm run build`
3. Убедитесь, что нет ошибок компиляции

Если есть ошибки TypeScript:
- Исправьте их локально
- Проверьте `bot/tsconfig.json`
- Убедитесь, что все зависимости установлены

### Решение 2: Проверка структуры файлов

В Railway проверьте логи и убедитесь, что:
- ✅ `dist/index.js` создается при сборке
- ✅ Все зависимости копируются правильно
- ✅ `package.json` находится в правильном месте

### Решение 3: Локальная проверка

Проверьте сборку локально:

```bash
cd bot
npm install
npm run build
ls -la dist/
```

Должен существовать файл `dist/index.js`.

### Решение 4: Проверка переменных окружения

В Railway Bot сервисе убедитесь, что установлены:

```env
TELEGRAM_BOT_TOKEN=your-bot-token
API_URL=https://your-backend.railway.app
NODE_ENV=production
```

**Важно:** `TELEGRAM_BOT_TOKEN` обязателен, без него бот не запустится.

### Решение 5: Проверка путей в Dockerfile

Убедитесь, что в Railway:
- **Root Directory:** пусто (или `bot` если Railway требует)
- **Dockerfile Path:** `bot/Dockerfile`
- Все файлы закоммичены в Git

### Решение 6: Проверка зависимостей

Убедитесь, что `package-lock.json` существует в корне репозитория:

```bash
# В корне репозитория
npm install
git add package-lock.json
git commit -m "Add package-lock.json"
git push
```

## Частые проблемы

### Проблема: "Cannot find module 'dist/index.js'"

**Причина:** TypeScript не скомпилировал файлы или путь неправильный

**Решение:**
1. Проверьте логи сборки - должна быть строка `npm run build`
2. Убедитесь, что `bot/tsconfig.json` настроен правильно
3. Проверьте, что `outDir` в tsconfig.json равен `./dist`

### Проблема: "TELEGRAM_BOT_TOKEN is not set"

**Причина:** Переменная окружения не установлена

**Решение:**
1. В Railway откройте Bot сервис → **Variables**
2. Добавьте `TELEGRAM_BOT_TOKEN=your-token`
3. Перезапустите сервис

### Проблема: "Cannot find module" при запуске

**Причина:** Зависимости не скопированы или установлены неправильно

**Решение:**
1. Проверьте, что `node_modules` копируется в runner stage
2. Убедитесь, что все workspace зависимости установлены
3. Проверьте логи установки зависимостей

### Проблема: Bot запускается, но не отвечает

**Причина:** Проблемы с подключением к Telegram или Backend API

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` - должен быть валидным
2. Проверьте `API_URL` - должен указывать на рабочий Backend
3. Проверьте логи бота на наличие ошибок подключения

## Проверка после исправления

После применения исправлений:

1. ✅ Build проходит успешно
2. ✅ `dist/index.js` создается
3. ✅ Все файлы скопированы правильно
4. ✅ Зависимости установлены
5. ✅ Переменные окружения установлены
6. ✅ Bot запускается и отвечает в Telegram

## Тестирование бота

После деплоя:

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением
4. Если бот не отвечает, проверьте логи в Railway

## Дополнительная отладка

Если проблема не решена:

1. **Проверьте полные логи** в Railway:
   - Deployments → последний деплой → View Logs
   - Найдите точную строку с ошибкой

2. **Проверьте локальную сборку**:
   ```bash
   cd bot
   npm install
   npm run build
   node dist/index.js
   ```

3. **Проверьте переменные окружения**:
   - Убедитесь, что все переменные установлены
   - Проверьте формат значений (без кавычек в Railway)

4. **Проверьте структуру проекта**:
   - Все файлы должны быть закоммичены
   - `package-lock.json` должен существовать
   - Структура workspace должна быть правильной







