# 🔧 Railway пропускает деплой - Решение проблемы "No changed files matched patterns"

## Проблема

Railway пропускает деплой с сообщением:
```
No changed files matched patterns: /backend/**
```

Или в GitHub Checks:
```
✅ Successful - No deployment needed - watched paths not modified
```

Это происходит, когда:
- Коммит изменяет файлы вне отслеживаемой директории (например, только `docs/`)
- Railway настроен на отслеживание изменений только в определенных директориях (Watched Paths)
- Пустой коммит или коммит с изменениями только в документации
- **Backend сервис имеет ограничения на Watched Paths, а Bot сервис - нет**

## 🔍 Причина

Railway автоматически определяет, нужно ли пересобирать приложение, проверяя, изменились ли файлы в указанных паттернах (например, `/backend/**`). Если изменений нет - деплой пропускается для экономии ресурсов.

## ✅ Решения

### Решение 1: Принудительный редеплой (Быстрое решение)

**Через Railway CLI:**
```bash
# Передеплоить последний успешный деплой
railway redeploy --service "@make-bot/backend" --yes

# Или без указания сервиса (если уже подключен)
railway redeploy --yes
```

**Через Railway Dashboard:**
1. Откройте Railway Dashboard → ваш сервис
2. Перейдите в **Deployments**
3. Найдите последний **успешный** деплой (зеленая галочка)
4. Нажмите на **три точки (⋮)** рядом с деплоем
5. Выберите **"Redeploy"**

### Решение 2: Убрать проверку паттернов файлов (Рекомендуется)

**Через Railway Dashboard:**
1. Откройте Railway Dashboard → ваш сервис
2. Перейдите в **Settings** → **Deploy**
3. Найдите раздел **"Watch Paths"** или **"Build Triggers"**
4. **Удалите** или **очистите** паттерны типа `/backend/**`
5. Или установите паттерн на корень: `/**` (отслеживать все изменения)
6. Сохраните изменения

**Альтернатива:** Если настройки "Watch Paths" нет в UI:
- Railway может использовать настройки из `railway.json`
- Проверьте, нет ли там ограничений на паттерны файлов

### Решение 3: Изменить настройки Source в Railway

1. **Railway Dashboard** → сервис → **Settings** → **Source**
2. Проверьте настройки:
   - **Root Directory:** должен быть пустым или `.` (корень репозитория)
   - **Watch Paths:** если есть - удалите или установите `/**`
3. Сохраните изменения

### Решение 4: Создать коммит с изменениями в backend

Если нужно задеплоить конкретный коммит, который не изменяет backend:

```bash
# Создать небольшое изменение в backend (например, комментарий)
echo "// Updated: $(date)" >> backend/src/index.ts

# Закоммитить
git add backend/src/index.ts
git commit -m "trigger backend deployment"

# Запушить
git push origin main
```

Или создать пустой файл-маркер:

```bash
# Создать файл-маркер
touch backend/.deploy-trigger
git add backend/.deploy-trigger
git commit -m "trigger deployment"
git push origin main
```

### Решение 5: Использовать Railway CLI с принудительным деплоем

```bash
# Переключиться на нужный коммит
git checkout <commit-hash>

# Задеплоить принудительно (Railway CLI может игнорировать проверку паттернов)
railway up --detach

# Вернуться на main
git checkout main
```

## 🎯 Рекомендуемый подход

**Для постоянного решения:**
1. Убрать ограничения на паттерны файлов в Railway Settings
2. Railway будет деплоить при любых изменениях в репозитории

**Для разового деплоя:**
1. Использовать `railway redeploy` для принудительного редеплоя
2. Или создать коммит с изменениями в backend

## 🔍 Проверка настроек

### Проверить текущие настройки через Railway CLI:

```bash
# Проверить статус подключения
railway status

# Проверить настройки сервиса (если доступно)
railway service
```

### Проверить через Railway Dashboard:

1. **Settings** → **Deploy** → проверьте **Watch Paths**
2. **Settings** → **Source** → проверьте **Root Directory** и **Branch**
3. **Settings** → **Build** → проверьте **Build Command** и **Dockerfile Path**

## 📝 Примеры конфигурации

### Правильная конфигурация railway.json (без ограничений):

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "/app/docker-entrypoint.sh"
  }
}
```

**Важно:** В `railway.json` нет настройки `watchPaths`, поэтому Railway использует настройки из Dashboard.

## ⚠️ Важные замечания

1. **Watch Paths** - это оптимизация, которая экономит ресурсы, пропуская деплои без изменений
2. Если убрать Watch Paths - Railway будет деплоить при **любых** изменениях (даже в `docs/`)
3. Для production это может быть избыточно, но гарантирует, что деплой не пропустится
4. Альтернатива - использовать принудительный редеплой только когда нужно

## 🚀 Быстрое решение прямо сейчас

```bash
# 1. Принудительный редеплой через CLI
railway redeploy --service "@make-bot/backend" --yes

# 2. Или через Dashboard:
# Railway → @make-bot/backend → Deployments → последний успешный деплой → Redeploy
```

## 📚 Дополнительные ресурсы

- [Railway Documentation - Deployments](https://docs.railway.app/deploy/builds)
- [Railway CLI - Redeploy](https://docs.railway.app/reference/cli#redeploy)
- [Подробное решение для Backend сервиса](./RAILWAY_BACKEND_WATCHED_PATHS_FIX.md) - если backend не деплоится, а bot деплоится

