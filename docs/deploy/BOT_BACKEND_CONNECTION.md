# 🔗 Настройка подключения бота к бэкенду

## Проблема

Бот показывает предупреждение `Backend API is not available`, даже если бэкенд работает.

## Причина

Бот использует переменную окружения `API_URL` для подключения к бэкенду. Если эта переменная не установлена или указан неправильный URL, бот не сможет подключиться к бэкенду.

## Решение

### Шаг 1: Определите URL бэкенда

В Railway у вас есть два варианта:

1. **Публичный URL бэкенда:**
   - `https://make-botbackend-production.up.railway.app`

2. **Внутренний Railway URL (если сервисы в одном проекте):**
   - Railway автоматически создает внутренние переменные окружения
   - Обычно это что-то вроде `http://@make-bot/backend:PORT` или через Service Variables

### Шаг 2: Установите переменную API_URL в боте

1. Откройте **Bot сервис** в Railway
2. Перейдите в **Variables**
3. Добавьте новую переменную:
   - **Name:** `API_URL`
   - **Value:** `https://make-botbackend-production.up.railway.app` (или ваш публичный URL бэкенда)

### Шаг 3: Перезапустите бота

После установки переменной:
1. Railway автоматически перезапустит бота
2. В логах бота вы увидите: `Checking backend API availability at: https://make-botbackend-production.up.railway.app`
3. Если бэкенд доступен, появится: `✅ Backend API is available`

## Проверка

После настройки проверьте логи бота:

**Успешно:**
```
Checking backend API availability at: https://make-botbackend-production.up.railway.app
✅ Backend API is available
```

**Проблема:**
```
Checking backend API availability at: http://localhost:3000
⚠️ Backend API is not available - some features may not work
Backend URL: http://localhost:3000
```

Если видите `http://localhost:3000`, значит переменная `API_URL` не установлена в Railway.

## Альтернатива: Использование Railway Service Variables

Если бот и бэкенд находятся в одном Railway проекте, можно использовать Service Variables:

1. В **Bot сервисе** → **Variables**
2. Добавьте переменную, которая ссылается на Backend сервис
3. Railway автоматически подставит правильный внутренний URL

Но проще всего использовать публичный URL бэкенда.

## Troubleshooting

### Бот все еще не видит бэкенд

1. **Проверьте, что бэкенд работает:**
   ```
   curl https://make-botbackend-production.up.railway.app/health
   ```
   Должен вернуть `{"status":"ok"}`

2. **Проверьте переменную API_URL в боте:**
   - Bot сервис → Variables → убедитесь, что `API_URL` установлена

3. **Проверьте логи бота:**
   - Должна быть строка `Checking backend API availability at: ...`
   - Если URL неправильный, исправьте переменную

4. **Проверьте сетевую доступность:**
   - Убедитесь, что бот может обращаться к публичному URL бэкенда
   - Проверьте, нет ли проблем с CORS или firewall

