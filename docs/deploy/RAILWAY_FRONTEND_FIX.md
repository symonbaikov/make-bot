# 🔧 Исправление проблем с деплоем Frontend на Railway

## Проблема

Frontend не запускается на Railway и выдает ошибку "Application failed to respond".

## Причины

1. **Неправильный Dockerfile путь** - Railway может не находить Dockerfile
2. **Неправильный Root Directory** - Railway может запускать из корня репозитория вместо папки frontend
3. **Отсутствие переменных окружения** - Frontend требует `VITE_API_URL`
4. **Проблемы с workspace структурой** - Dockerfile должен работать с monorepo структурой

## ✅ Решение

### Шаг 1: Настройка сервиса в Railway

1. Откройте ваш проект в Railway
2. Найдите сервис `@make-bot/frontend` или создайте новый
3. Перейдите в **Settings** → **Service Settings**
4. Убедитесь, что:
   - **Root Directory:** пусто (или `frontend` если Railway требует)
   - **Dockerfile Path:** `frontend/Dockerfile`
   - **Build Command:** (оставьте пустым, Dockerfile сам соберет)
   - **Start Command:** (оставьте пустым, Dockerfile сам запустит)

### Шаг 2: Настройка переменных окружения

В Railway перейдите в **Variables** и добавьте:

```env
VITE_API_URL=https://your-backend.railway.app
```

**Важно:** Замените `your-backend.railway.app` на реальный URL вашего backend сервиса.

### Шаг 3: Проверка конфигурации

Убедитесь, что файл `frontend/railway.json` существует и содержит:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "frontend/Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### Шаг 4: Пересборка и деплой

1. В Railway откройте ваш Frontend сервис
2. Перейдите в **Deployments**
3. Нажмите **Redeploy** или сделайте новый commit в GitHub
4. Railway автоматически пересоберет и задеплоит приложение

### Шаг 5: Проверка логов

После деплоя проверьте логи:

1. Откройте **Deployments** → выберите последний деплой
2. Нажмите **View Logs**
3. Убедитесь, что:
   - ✅ Build прошел успешно
   - ✅ Nginx запустился
   - ✅ Нет ошибок с путями или зависимостями

## 🔍 Troubleshooting

### Ошибка: "Cannot find Dockerfile"

**Решение:**
- Убедитесь, что в Railway Settings указан правильный путь: `frontend/Dockerfile`
- Или создайте сервис заново и укажите Root Directory как `frontend`

### Ошибка: "npm install failed"

**Решение:**
- Проверьте, что `package-lock.json` существует в корне репозитория
- Убедитесь, что workspace структура правильная

### Ошибка: "Build failed - cannot find module"

**Решение:**
- Проверьте логи сборки - возможно, не все зависимости установились
- Убедитесь, что используется `npm install --legacy-peer-deps`

### Ошибка: "Application failed to respond"

**Решение:**
1. Проверьте, что nginx запустился (логи должны показывать "nginx started")
2. Проверьте, что порт 80 доступен (Railway автоматически проксирует)
3. Проверьте health endpoint: `https://your-frontend.railway.app/health`
4. Убедитесь, что переменная `VITE_API_URL` установлена правильно

### Frontend работает, но не подключается к Backend

**Решение:**
1. Проверьте переменную `VITE_API_URL` в Railway
2. Убедитесь, что Backend доступен публично
3. Проверьте CORS настройки в Backend (должен разрешать ваш Frontend домен)

## 📋 Чеклист

- [ ] Dockerfile находится в `frontend/Dockerfile`
- [ ] `frontend/railway.json` создан и настроен
- [ ] Переменная `VITE_API_URL` установлена в Railway
- [ ] Root Directory настроен правильно (пусто или `frontend`)
- [ ] Build прошел успешно (проверьте логи)
- [ ] Nginx запустился (проверьте логи)
- [ ] Health endpoint отвечает: `/health`
- [ ] Frontend доступен по публичному URL

## 🎯 Ожидаемый результат

После исправления:

1. ✅ Build проходит успешно
2. ✅ Nginx запускается и слушает порт 80
3. ✅ Frontend доступен по публичному URL Railway
4. ✅ Health endpoint отвечает: `https://your-frontend.railway.app/health`
5. ✅ Frontend подключается к Backend API

## 📞 Дополнительная помощь

Если проблема не решена:

1. Проверьте полные логи деплоя в Railway
2. Убедитесь, что все файлы закоммичены в Git
3. Попробуйте пересоздать сервис в Railway
4. Проверьте документацию Railway: https://docs.railway.app

