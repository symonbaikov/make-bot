# 🔗 Как получить публичный DATABASE_URL из Railway

## Проблема

При попытке подключиться к базе данных локально возникает ошибка:
```
Error: P1001: Can't reach database server at `postgres.railway.internal:5432`
```

Это происходит потому, что `postgres.railway.internal` - это **внутренний адрес**, доступный только внутри Railway сети.

## ✅ Решение: Использовать публичный DATABASE_URL

### Способ 1: Через Railway UI (САМЫЙ ПРОСТОЙ)

1. **Откройте PostgreSQL сервис** в Railway
2. **Нажмите кнопку "Connect"** (с иконкой молнии)
3. **Выберите "Public Network"** → **"TCP Proxy"**
4. Railway создаст публичный URL типа:
   ```
   postgres.railway.app:5432
   ```
5. **Соберите полный DATABASE_URL:**
   ```
   postgresql://postgres:PASSWORD@postgres.railway.app:5432/railway
   ```
   Где `PASSWORD` - пароль из PostgreSQL → Variables → `POSTGRES_PASSWORD`

### Способ 2: Из Variables (если есть публичный URL)

1. **Откройте PostgreSQL сервис** → **Variables**
2. **Найдите `DATABASE_URL`** или `POSTGRES_URL`
3. **Проверьте, содержит ли он публичный хост:**
   - ✅ Хорошо: `postgres.railway.app` или `containers-us-west-xxx.railway.app`
   - ❌ Плохо: `postgres.railway.internal` (внутренний адрес)

4. **Если содержит `railway.internal`:**
   - Замените на публичный хост из способа 1
   - Или используйте способ 3

### Способ 3: Через Railway CLI

1. **Получите переменные:**
   ```bash
   railway variables --service postgres
   ```

2. **Найдите `DATABASE_URL`** или соберите из отдельных переменных:
   ```bash
   railway variables --service postgres --json | jq '.[] | select(.name == "DATABASE_URL")'
   ```

3. **Если URL содержит `railway.internal`:**
   - Создайте TCP Proxy через UI (способ 1)
   - Или используйте способ 4

### Способ 4: Создать TCP Proxy через Railway CLI

```bash
# Создать TCP Proxy для PostgreSQL
railway connect postgres
```

Это создаст публичный URL для подключения.

## Применение миграций с публичным DATABASE_URL

После получения публичного URL:

```bash
cd backend

# Установите DATABASE_URL
export DATABASE_URL="postgresql://postgres:PASSWORD@PUBLIC_HOST:PORT/railway"

# Примените миграции
npx prisma db push --skip-generate --accept-data-loss

# Создайте админа
npm run db:seed
```

## Проверка подключения

Перед применением миграций проверьте подключение:

```bash
# Установите DATABASE_URL
export DATABASE_URL="postgresql://postgres:PASSWORD@PUBLIC_HOST:PORT/railway"

# Проверьте подключение
npx prisma db execute --stdin <<< "SELECT 1;"
```

Если команда выполнилась без ошибок - подключение работает!

## Безопасность

⚠️ **ВАЖНО:** Публичный DATABASE_URL доступен из интернета!

- Используйте его только для локальной разработки
- Не коммитьте его в Git
- После применения миграций можно удалить TCP Proxy
- В Railway контейнерах используйте внутренний `railway.internal` адрес

## Альтернатива: Применить миграции внутри Railway контейнера

Если не хотите создавать публичный доступ:

1. **Подключитесь к Backend контейнеру через Railway:**
   ```bash
   railway shell --service @make-bot/backend
   ```

2. **Внутри контейнера:**
   ```bash
   cd /app
   npx prisma db push --skip-generate --accept-data-loss
   npm run db:seed
   ```

Это использует внутренний `railway.internal` адрес, который доступен внутри Railway сети.

