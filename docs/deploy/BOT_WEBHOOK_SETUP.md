# 🤖 Настройка Telegram Webhook для бота

## Проблема

Бот не отвечает, потому что `TELEGRAM_WEBHOOK_URL` настроен неправильно. Telegram требует полный HTTPS URL с путем `/webhook`.

## Решение

### Шаг 1: Получите публичный URL бота из Railway

1. Откройте **Bot сервис** в Railway
2. Перейдите в **Settings** → **Networking**
3. Нажмите **Generate Domain** (если домен еще не создан)
4. Скопируйте публичный URL (например, `https://your-bot-name.up.railway.app`)

### Шаг 2: Установите правильный `TELEGRAM_WEBHOOK_URL`

1. В Railway для **Bot сервиса** откройте **Variables**
2. Найдите или создайте переменную `TELEGRAM_WEBHOOK_URL`
3. Установите значение:
   ```
   https://your-bot-name.up.railway.app/webhook
   ```
   
   **Важно:** 
   - Должен быть полный URL с `https://`
   - Должен заканчиваться на `/webhook`
   - Используйте публичный домен бота, а не бэкенда!

### Шаг 3: Проверьте другие переменные

Убедитесь, что установлены:

- `TELEGRAM_BOT_TOKEN` - токен бота от BotFather
- `API_URL` - URL бэкенда (например, `https://make-botbackend-production.up.railway.app`)
- `NODE_ENV=production` - для продакшена
- `PORT` - Railway установит автоматически

### Шаг 4: Перезапустите бот

После установки правильного `TELEGRAM_WEBHOOK_URL` Railway автоматически перезапустит бот.

### Шаг 5: Проверьте логи

В логах бота должно быть:

```
Starting bot in webhook mode: https://your-bot-name.up.railway.app/webhook
Webhook server listening on port 8080
Deleted existing webhook (if any)
Webhook set to: https://your-bot-name.up.railway.app/webhook
```

Если видите ошибку:
```
Failed to set webhook
```

Проверьте:
- URL правильный и доступен по HTTPS
- Telegram может достучаться до вашего сервера
- Порт правильно настроен

## Пример правильной настройки

**Неправильно:**
```
TELEGRAM_WEBHOOK_URL=make-botbackend-production.up.railway.app
```

**Правильно:**
```
TELEGRAM_WEBHOOK_URL=https://your-bot-name.up.railway.app/webhook
```

## Troubleshooting

### Бот не отвечает

1. **Проверьте логи бота:**
   - Должна быть строка `Webhook set to: ...`
   - Если видите `Failed to set webhook`, проверьте URL

2. **Проверьте доступность webhook:**
   ```bash
   curl https://your-bot-name.up.railway.app/webhook
   ```
   Должен вернуть ошибку (это нормально, так как нужен POST запрос)

3. **Проверьте health endpoint:**
   ```bash
   curl https://your-bot-name.up.railway.app/health
   ```
   Должен вернуть `{"status":"ok","mode":"webhook"}`

### Webhook не устанавливается

1. **Проверьте, что бот запущен:**
   - В Railway проверьте статус сервиса
   - Проверьте логи на наличие ошибок

2. **Проверьте формат URL:**
   - Должен начинаться с `https://`
   - Должен заканчиваться на `/webhook`
   - Не должно быть пробелов или лишних символов

3. **Проверьте токен бота:**
   - Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
   - Проверьте токен в BotFather

### Использование polling вместо webhook (временно)

Если нужно быстро протестировать бота без webhook:

1. Установите переменную:
   ```
   ALLOW_POLLING_IN_PRODUCTION=true
   ```

2. Удалите или закомментируйте `TELEGRAM_WEBHOOK_URL`

**Внимание:** Polling не рекомендуется для продакшена, используйте только для тестирования!

