# 🔧 Исправление проблемы с портом в Railway

## Проблема

Railway автоматически устанавливает переменную окружения `PORT=8080` для всех сервисов. Если в Dockerfile жестко задан `ENV PORT=3000`, это может создать конфликт.

## Решение

### Вариант 1: Настроить домен на порт 8080 (РЕКОМЕНДУЕТСЯ)

1. **Откройте Backend сервис** в Railway
2. **Перейдите в Settings** → **Networking**
3. **В разделе "Public Networking"** найдите настройки домена
4. **Измените порт на 8080** (или удалите домен и создайте заново с портом 8080)
5. **Сохраните изменения**

### Вариант 2: Удалить жестко заданный PORT из Dockerfile

Dockerfile уже обновлен, чтобы использовать `PORT` из окружения Railway. Если Railway устанавливает `PORT=8080`, приложение будет слушать на порту 8080.

## Проверка

После настройки:

1. **Проверьте логи:**
   - Откройте Deploy Logs
   - Должно быть: `✅ Server running on port 8080`

2. **Проверьте домен:**
   - Откройте Settings → Networking
   - Убедитесь, что домен настроен на порт 8080

3. **Проверьте приложение:**
   ```
   https://your-backend.railway.app/health
   ```
   Должен вернуть `{"status":"ok"}`

## Почему Railway использует порт 8080?

Railway автоматически устанавливает `PORT=8080` для всех сервисов. Это стандартная практика для платформ PaaS (Platform as a Service). Приложение должно использовать переменную окружения `PORT`, а не жестко заданный порт.

## Что было исправлено

1. ✅ Dockerfile теперь использует `PORT` из окружения: `ENV PORT=${PORT:-3000}`
2. ✅ Приложение читает `PORT` из окружения: `const PORT = process.env.PORT || 3000`
3. ✅ Dockerfile экспонирует оба порта: `EXPOSE 3000 8080`

Теперь приложение будет работать как на Railway (порт 8080), так и локально (порт 3000 по умолчанию).

