# 🚀 Быстрый старт: Настройка Railway с нуля

## Шаг 1: Создать PostgreSQL сервис

1. **Откройте Railway** → ваш проект
2. **Нажмите "+ New"** → **Database** → **Add PostgreSQL**
3. Дождитесь создания (1-2 минуты)
4. **Откройте PostgreSQL сервис** → **Variables**
5. **Скопируйте значение `DATABASE_URL`**

## Шаг 2: Настроить Backend сервис

1. **Откройте `@make-bot/backend`** сервис
2. **Перейдите в Variables**
3. **Создайте переменные:**

### Обязательные переменные:

| Переменная | Значение | Откуда взять |
|-----------|----------|--------------|
| `DATABASE_URL` | Скопируйте из PostgreSQL сервиса | PostgreSQL → Variables → DATABASE_URL |
| `JWT_SECRET` | Случайная строка (минимум 32 символа) | Сгенерируйте: `openssl rand -hex 32` |
| `NODE_ENV` | `production` | Установите вручную |

### Опциональные переменные:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `CORS_ORIGIN` | `https://your-domain.com` | Разрешенные origins для CORS |
| `SENTRY_DSN` | Ваш Sentry DSN | Для отслеживания ошибок |
| `MAKE_WEBHOOK_URL` | URL webhook в Make | Для интеграции с Make |

## Шаг 3: Настроить Bot сервис

1. **Откройте `@make-bot/bot`** сервис
2. **Перейдите в Variables**
3. **Создайте переменные:**

| Переменная | Значение | Откуда взять |
|-----------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен вашего бота | От @BotFather в Telegram |
| `TELEGRAM_WEBHOOK_URL` | `https://your-bot-url.railway.app/webhook` | URL вашего бота на Railway |
| `API_URL` | `https://your-backend-url.railway.app/api` | URL вашего Backend сервиса |
| `DATABASE_URL` | Скопируйте из PostgreSQL сервиса | PostgreSQL → Variables → DATABASE_URL |

## Шаг 4: Удалить старый Frontend сервис (если есть)

1. **Найдите `@make-bot/frontend`** сервис
2. **Откройте его** → **Settings** → **Delete Service**
3. **Подтвердите удаление**

**Почему?** Фронтенд теперь часть монолитного Backend сервиса.

## Шаг 5: Пересобрать сервисы

1. **Backend:** Откройте `@make-bot/backend` → **Deploy** → **Redeploy**
2. **Bot:** Откройте `@make-bot/bot` → **Deploy** → **Redeploy**

## Шаг 6: Проверка

### Проверьте Backend:

1. **Health check:**
   ```
   https://your-backend.railway.app/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. **Фронтенд:**
   ```
   https://your-backend.railway.app/
   ```
   Должен открыться админ-панель

3. **API:**
   ```
   https://your-backend.railway.app/api/admin/auth/login
   ```
   Должен вернуть JSON (даже если ошибка валидации - это нормально)

### Проверьте Bot:

1. **Откройте логи Bot сервиса**
2. Должно быть сообщение: `Bot started successfully`
3. **Проверьте в Telegram:** Отправьте `/start` боту

## Troubleshooting

### Backend падает с ошибкой DATABASE_URL

**Решение:**
1. Убедитесь, что PostgreSQL сервис создан
2. Скопируйте `DATABASE_URL` из PostgreSQL → Variables
3. Вставьте в Backend → Variables
4. Пересоберите Backend

### Bot падает с ошибкой

**Проверьте:**
1. `TELEGRAM_BOT_TOKEN` установлен и правильный
2. `API_URL` указывает на правильный Backend URL
3. `DATABASE_URL` установлен

### Фронтенд не открывается

**Проверьте:**
1. Backend сервис запущен (не Crashed)
2. Логи Backend не содержат ошибок
3. URL правильный (используйте Backend URL, не Frontend)

## Итоговая структура сервисов

После настройки у вас должно быть **3 сервиса**:

1. **PostgreSQL** - база данных
2. **@make-bot/backend** - монолит (бэкенд + фронтенд)
3. **@make-bot/bot** - Telegram бот

## Следующие шаги

1. ✅ Настройте Make сценарии (см. `docs/MAKE_SETUP_GUIDE.md`)
2. ✅ Создайте первого админа в базе данных (см. `backend/prisma/seed.ts`)
3. ✅ Настройте домены (опционально)

