# 🔄 Миграция на монолитную архитектуру

## Проблема

Railway все еще деплоит старую архитектуру с тремя отдельными сервисами:
- `@make-bot/frontend` - отдельный сервис (больше не нужен)
- `@make-bot/bot` - отдельный сервис (нужен)
- `@make-bot/backend` - отдельный сервис (нужен, но теперь монолит)

## Решение: Миграция на монолит

Фронтенд теперь является частью бэкенда и деплоится вместе с ним.

## ⚠️ ВАЖНО: Что нужно сделать в Railway

### Шаг 1: Удалить старый Frontend сервис

1. Откройте Railway → ваш проект
2. Найдите сервис `@make-bot/frontend`
3. Откройте его → **Settings** → **Delete Service**
4. Подтвердите удаление

**⚠️ ВАЖНО:** Не удаляйте `@make-bot/bot` - он нужен!

### Шаг 2: Обновить Backend сервис

1. Откройте сервис `@make-bot/backend`
2. Перейдите в **Settings** → **Service Settings**
3. Проверьте настройки **Build & Deploy**:
   - **Root Directory:** оставьте **ПУСТЫМ** (или `.`)
   - **Dockerfile Path:** `backend/Dockerfile`
   - **Build Command:** оставьте пустым
   - **Start Command:** оставьте пустым (используется из Dockerfile)
4. Сохраните изменения

### Шаг 3: Настроить переменные окружения для Backend

**КРИТИЧНО:** Убедитесь, что установлены все необходимые переменные:

1. Откройте сервис `@make-bot/backend`
2. Перейдите в **Variables**
3. Убедитесь, что установлены:

#### Обязательные переменные:
- `DATABASE_URL` - строка подключения к PostgreSQL (из Railway PostgreSQL сервиса)
- `JWT_SECRET` - секретный ключ для JWT токенов (сгенерируйте случайную строку)
- `NODE_ENV=production`

#### Опциональные переменные:
- `CORS_ORIGIN` - разрешенные origins для CORS (например, `https://your-domain.com`)
- `SENTRY_DSN` - DSN для Sentry (если используете)
- `MAKE_WEBHOOK_URL` - URL для отправки webhooks в Make
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (если нужен в бэкенде)
- `TELEGRAM_WEBHOOK_URL` - URL для webhook бота (если используете webhook)

### Шаг 4: Получить DATABASE_URL из Railway PostgreSQL

Если у вас есть PostgreSQL сервис в Railway:

1. Откройте PostgreSQL сервис в Railway
2. Перейдите в **Variables**
3. Найдите `DATABASE_URL` или `POSTGRES_URL`
4. Скопируйте значение
5. Вставьте в переменные Backend сервиса как `DATABASE_URL`

Если PostgreSQL сервиса нет:

1. Создайте новый PostgreSQL сервис в Railway
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Скопируйте `DATABASE_URL` в переменные Backend сервиса

### Шаг 5: Пересобрать Backend

1. Откройте сервис `@make-bot/backend`
2. Нажмите **Deploy** → **Redeploy**
3. Дождитесь завершения сборки

### Шаг 6: Проверка

После деплоя проверьте:

1. **Health check:**
   ```
   https://your-backend.railway.app/health
   ```
   Должен вернуть `{"status":"ok"}`

2. **Фронтенд:**
   ```
   https://your-backend.railway.app/
   ```
   Должен открыться фронтенд (React приложение)

3. **API:**
   ```
   https://your-backend.railway.app/api/admin/auth/login
   ```
   Должен вернуть JSON (даже если ошибка валидации - это нормально)

## Итоговая структура сервисов в Railway

После миграции у вас должно быть **2 сервиса**:

1. **@make-bot/backend** - монолит (бэкенд + фронтенд)
   - Dockerfile: `backend/Dockerfile`
   - Root Directory: пусто (корень репозитория)
   - Раздает фронтенд и API

2. **@make-bot/bot** - Telegram бот
   - Dockerfile: `bot/Dockerfile`
   - Root Directory: пусто (корень репозитория)
   - Обрабатывает Telegram сообщения

## Troubleshooting

### Backend падает с ошибкой "DATABASE_URL is required"

**Решение:** Установите переменную `DATABASE_URL` в настройках Backend сервиса.

### Фронтенд не открывается

**Проверьте:**
1. Логи сборки - фронтенд должен собраться успешно
2. Логи деплоя - не должно быть ошибок при запуске
3. Убедитесь, что `NODE_ENV=production`

### API запросы не работают

**Проверьте:**
1. CORS настройки (`CORS_ORIGIN`)
2. Логи бэкенда на наличие ошибок
3. Правильность URL (должен быть `/api/...`)

### Старый Frontend сервис все еще виден

**Решение:** Удалите его вручную в Railway UI (Settings → Delete Service)

## Откат к старой архитектуре

Если нужно вернуться к раздельному деплою:

1. Восстановите `frontend/railway.json` (переименуйте обратно из `.disabled`)
2. Создайте новый Frontend сервис в Railway
3. Удалите код раздачи статики из `backend/src/index.ts`
4. Настройте прокси между фронтендом и бэкендом

