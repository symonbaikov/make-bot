# 🔗 Настройка Make Webhook в бэкенде

## Проверка текущей настройки

Бэкенд автоматически отправляет данные в Make webhook после получения данных от бота. Проверьте, что переменная окружения установлена правильно.

## Как это работает

1. **Пользователь отправляет данные в бот** (email, имя, телефон)
2. **Бот отправляет данные в бэкенд** через `POST /api/webhook/bot`
3. **Бэкенд сохраняет данные в БД**
4. **Бэкенд отправляет данные в Make** через webhook URL

## Настройка Make Webhook URL

### Шаг 1: Установите переменную в Railway

1. Откройте **Backend сервис** в Railway
2. Перейдите в **Variables**
3. Добавьте или проверьте переменную:
   - **Name:** `MAKE_WEBHOOK_URL`
   - **Value:** `https://hook.eu2.make.com/yby89eor226asakp3puq0osigns5jrg8`

### Шаг 2: Перезапустите бэкенд

После установки переменной Railway автоматически перезапустит бэкенд.

### Шаг 3: Проверьте логи

После перезапуска в логах бэкенда должно быть:

**Если переменная установлена:**
```
Make webhook URL configured { url: 'https://hook.eu2.make.com/****' }
```

**Если переменная НЕ установлена:**
```
MAKE_WEBHOOK_URL is not configured - Make webhooks will be skipped
To enable Make integration, set MAKE_WEBHOOK_URL in Railway environment variables
```

## Формат данных, отправляемых в Make

Когда пользователь отправляет данные через бота, бэкенд отправляет в Make следующий JSON:

```json
{
  "event": "bot_email_collected",
  "sessionId": "test-session-001",
  "email": "user@example.com",
  "tgUserId": "123456789",
  "firstName": "John",
  "lastName": "Doe",
  "phoneNumber": "+1234567890",
  "plan": "STANDARD",
  "amount": 99.99
}
```

## Проверка работы

### Способ 1: Проверка логов бэкенда

После отправки данных через бота, в логах бэкенда должно быть:

```
Sending webhook to Make { sessionId: '...', event: 'bot_email_collected', webhookUrl: 'https://hook.eu2.make.com/****' }
Webhook sent to Make successfully { sessionId: '...' }
```

Если видите ошибки:
```
Failed to send webhook to Make { error: '...', status: ..., sessionId: '...' }
```

### Способ 2: Проверка в Make

1. Откройте ваш сценарий в Make
2. Проверьте историю выполнения (Execution history)
3. Должны быть успешные выполнения webhook модуля

### Способ 3: Проверка в базе данных

В таблице `actions` должны быть записи:
- `type: WEBHOOK_SENT` - успешная отправка
- `type: WEBHOOK_FAILED` - неудачная отправка (после всех попыток)

## Troubleshooting

### Webhook не отправляется

1. **Проверьте переменную `MAKE_WEBHOOK_URL`:**
   - Backend сервис → Variables → убедитесь, что переменная установлена
   - URL должен быть полным: `https://hook.eu2.make.com/yby89eor226asakp3puq0osigns5jrg8`

2. **Проверьте логи бэкенда:**
   - Должна быть строка `Make webhook URL configured`
   - Если видите `MAKE_WEBHOOK_URL is not configured`, значит переменная не установлена

3. **Проверьте доступность Make webhook:**
   ```bash
   curl -X POST https://hook.eu2.make.com/yby89eor226asakp3puq0osigns5jrg8 \
     -H "Content-Type: application/json" \
     -d '{"test": "data"}'
   ```
   Должен вернуть `Accepted` или `200 OK`

### Webhook отправляется, но Make не получает данные

1. **Проверьте формат данных в Make:**
   - Убедитесь, что webhook модуль в Make настроен правильно
   - Проверьте, что модуль ожидает JSON формат

2. **Проверьте логи Make:**
   - В Make откройте Execution history
   - Проверьте, есть ли ошибки в webhook модуле

3. **Проверьте логи бэкенда:**
   - Если видите `Failed to send webhook to Make`, проверьте статус код ошибки
   - Проверьте, что Make webhook URL правильный и доступен

## Важные замечания

- Бэкенд автоматически повторяет отправку webhook до 3 раз при ошибке
- Ошибки отправки webhook не блокируют основной процесс (сохранение данных в БД)
- Все попытки отправки логируются в таблицу `actions`

