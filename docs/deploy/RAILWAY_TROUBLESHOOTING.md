# 🔧 Troubleshooting Railway Deployment

## Проблема: Таблица web_users не существует

Если вы видите ошибки в логах PostgreSQL:
```
ERROR: relation "public.web_users" does not exist
```

Это означает, что миграции Prisma не были применены к базе данных.

## ✅ Решение

### Шаг 1: Проверьте DATABASE_URL

1. **Откройте Backend сервис** в Railway → **Variables**
2. **Убедитесь, что `DATABASE_URL` установлен** и содержит реальный URL из PostgreSQL сервиса
3. **Формат должен быть:**
   ```
   postgresql://postgres:password@host:port/database?sslmode=require
   ```

### Шаг 2: Пересоберите Backend

1. **Откройте Backend сервис** → **Deploy** → **Redeploy**
2. **Проверьте Deploy Logs** — должны быть сообщения:
   - `Database ready!`
   - `Running database migrations...`
   - `Applying database schema with db push...`
   - `✅ Database schema applied successfully!`
   - `Running database seed...`

### Шаг 3: Проверьте логи на ошибки

Если миграции не применяются, проверьте логи:

1. **Откройте Deploy Logs**
2. **Найдите сообщения про миграции:**
   - Если видите `ERROR: Database push failed!` — проверьте `DATABASE_URL`
   - Если видите `prisma directory not found` — проблема с копированием файлов в Docker

### Шаг 4: Ручное применение миграций (если автоматически не работает)

Если автоматические миграции не работают, можно применить их вручную:

1. **Подключитесь к Railway PostgreSQL через CLI:**
   ```bash
   # Установите Railway CLI
   npm i -g @railway/cli
   
   # Войдите в Railway
   railway login
   
   # Подключитесь к проекту
   railway link
   
   # Получите DATABASE_URL
   railway variables
   ```

2. **Примените миграции локально:**
   ```bash
   # Установите DATABASE_URL из Railway
   export DATABASE_URL="postgresql://..."
   
   # Примените схему
   cd backend
   npx prisma db push
   
   # Запустите seed
   npm run db:seed
   ```

## Проверка после исправления

После применения миграций проверьте:

1. **Health check:**
   ```
   https://your-backend.railway.app/health
   ```
   Должен вернуть `{"status":"ok"}`

2. **Попробуйте залогиниться:**
   - Email: `admin@example.com`
   - Password: `admin123`
   - Должен работать вход

3. **Проверьте таблицы в PostgreSQL:**
   - Откройте PostgreSQL сервис → **Query**
   - Выполните:
     ```sql
     SELECT table_name FROM information_schema.tables 
     WHERE table_schema = 'public';
     ```
   - Должны быть таблицы: `web_users`, `users`, `sessions`, `actions`

## Частые проблемы

### DATABASE_URL содержит placeholder

**Симптомы:**
- Логи показывают `localhost:5433`
- Ошибки подключения к базе данных

**Решение:**
- Скопируйте реальный `DATABASE_URL` из PostgreSQL сервиса
- Вставьте в Variables Backend сервиса

### Миграции не применяются

**Симптомы:**
- В логах нет сообщений про миграции
- Таблицы не создаются

**Решение:**
- Проверьте, что `prisma/schema.prisma` существует
- Проверьте, что `DATABASE_URL` установлен
- Пересоберите Backend сервис

### Seed не создает админа

**Симптомы:**
- Таблицы созданы, но нет пользователя `admin@example.com`

**Решение:**
- Проверьте логи seed (должно быть `Created admin user`)
- Запустите seed вручную через Railway CLI или создайте пользователя через Prisma Studio

## Дополнительная помощь

Если проблема не решается:

1. **Проверьте все логи:**
   - Build Logs — ошибки сборки
   - Deploy Logs — ошибки запуска
   - PostgreSQL Logs — ошибки базы данных

2. **Проверьте переменные окружения:**
   - `DATABASE_URL` — должен быть установлен
   - `NODE_ENV` — должен быть `production`
   - `JWT_SECRET` — должен быть установлен

3. **Проверьте структуру проекта:**
   - `backend/prisma/schema.prisma` — должен существовать
   - `backend/prisma/seed.ts` — должен существовать

