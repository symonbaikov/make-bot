# 🚀 Быстрое исправление проблем с деплоем на Railway

## Проблема

Frontend и Bot не запускаются на Railway с ошибками типа:
- `ERROR: package.json not found!`
- `at require (node:internal/modules/helpers:177:18)`

## ⚡ Быстрое решение (5 минут)

### Для каждого сервиса (Frontend, Bot, Backend):

1. **Откройте Railway** → ваш проект
2. **Выберите сервис** (например, `@make-bot/frontend`)
3. **Settings** → **Service Settings**
4. **Build & Deploy** секция:
   - **Root Directory:** оставьте **ПУСТЫМ** (или установите `.`)
   - **Dockerfile Path:** `frontend/Dockerfile` (для frontend) или `bot/Dockerfile` (для bot)
   - **Build Command:** оставьте пустым
   - **Start Command:** оставьте пустым
5. **Сохраните изменения**
6. **Redeploy** сервис

### Проверка:

После redeploy проверьте логи:
- **Build Logs** должны показывать успешную сборку
- **Deploy Logs** должны показывать запуск сервиса
- Не должно быть ошибок типа "package.json not found"

## 📝 Что было исправлено

1. ✅ Добавлены проверки в Dockerfile для лучшей диагностики
2. ✅ Создан `bot/railway.json` для правильной конфигурации
3. ✅ Улучшены Dockerfile с проверками на каждом этапе

## 🔍 Если не помогло

См. подробную инструкцию: [RAILWAY_BUILD_CONTEXT_FIX.md](./RAILWAY_BUILD_CONTEXT_FIX.md)

