# 🚀 Принудительный редеплой в Railway (как в Vercel)

## Вопрос

Почему нельзя просто пересобрать весь проект с последнего коммита в Railway, как это делается в Vercel?

## ✅ Ответ: Можно! И это очень просто

Railway поддерживает принудительный редеплой, точно так же как Vercel. Есть несколько способов:

## 🎯 Способ 1: Задеплоить новый коммит через Railway CLI (РЕКОМЕНДУЕТСЯ)

### Для Backend сервиса:

```bash
# Переключиться на нужный коммит (который еще не задеплоен)
git checkout <commit-hash>

# Задеплоить этот коммит
railway up --detach

# Вернуться на main
git checkout main
```

**Или из корня проекта:**

```bash
# Задеплоить конкретный коммит
git checkout <commit-hash>
railway up --service "@make-bot/backend" --detach
git checkout main
```

### Пример:

```bash
# Задеплоить коммит fa2e577
git checkout fa2e577
railway up --service "@make-bot/backend" --detach
git checkout main
```

## 🎯 Способ 2: Через Railway Dashboard (Redeploy последнего успешного)

⚠️ **ВНИМАНИЕ:** Это передеплоит последний успешный деплой, а не новый коммит!

### Для Backend сервиса:

1. **Откройте Railway Dashboard** → ваш проект → `@make-bot/backend`
2. **Перейдите в Deployments** (вкладка вверху)
3. **Найдите последний успешный деплой** (с зеленой галочкой)
4. **Нажмите на три точки (⋮)** справа от деплоя
5. **Выберите "Redeploy"**
6. Railway пересоберет проект с **того коммита**, который был в последнем успешном деплое

**Это НЕ задеплоит новый коммит!** Только пересоберет старый.

### Для Bot сервиса:

То же самое:

1. **Railway Dashboard** → `@make-bot/bot` → **Deployments**
2. **Последний успешный деплой** → **⋮** → **Redeploy**

## 🎯 Способ 3: Через Railway CLI - Redeploy (Передеплой старого коммита)

### Принудительный редеплой Backend (старый коммит):

⚠️ **ВНИМАНИЕ:** Это передеплоит последний успешный деплой, а не новый коммит!

```bash
# Переключиться на backend сервис (если нужно)
railway service link  # выберите @make-bot/backend

# Принудительный редеплой последнего успешного деплоя
railway redeploy --yes
```

### Принудительный редеплой Bot:

```bash
# Переключиться на bot сервис
cd bot
railway service link  # выберите @make-bot/bot

# Или из корня с указанием сервиса
railway redeploy --service "@make-bot/bot" --yes
```

### Принудительный редеплой всех сервисов:

```bash
# Backend
railway redeploy --service "@make-bot/backend" --yes

# Bot
railway redeploy --service "@make-bot/bot" --yes
```

## 🎯 Способ 4: Задеплоить новый коммит через пустой коммит (НЕ РАБОТАЕТ с Watched Paths)

⚠️ **Это НЕ работает**, если у вас настроены Watched Paths - Railway все равно пропустит деплой!

```bash
# Создать пустой коммит
git commit --allow-empty -m "force redeploy"

# Запушить
git push origin main
```

**Railway пропустит этот деплой**, если изменения не в отслеживаемых путях.

**Решение:** Используйте Способ 1 (через `railway up` с checkout на нужный коммит).

## 🔄 Сравнение: Vercel vs Railway

| Действие                          | Vercel                    | Railway                             |
| --------------------------------- | ------------------------- | ----------------------------------- |
| **Автоматический деплой**         | ✅ Всегда при push        | ⚠️ Может пропустить (Watched Paths) |
| **Принудительный редеплой**       | ✅ Deployments → Redeploy | ✅ Deployments → Redeploy           |
| **CLI редеплой**                  | ✅ `vercel --prod`        | ✅ `railway redeploy`               |
| **Редиплой с последнего коммита** | ✅ Да                     | ✅ Да                               |

**Вывод:** Railway поддерживает принудительный редеплой точно так же, как Vercel!

## 💡 Почему Railway пропускает автоматический деплой?

Railway использует оптимизацию "Watched Paths" для экономии ресурсов:

- Если изменения не в отслеживаемых путях - деплой пропускается
- Это экономит время и деньги на бесплатных планах

**Но принудительный редеплой всегда работает**, независимо от Watched Paths!

## 🚀 Рекомендуемый подход

### Вариант 1: Убрать Watched Paths (Постоянное решение)

Чтобы Railway работал как Vercel (всегда деплоить при push):

1. **Railway Dashboard** → `@make-bot/backend` → **Settings** → **Deploy**
2. **Найдите "Watched Paths"** → **очистите** или установите `/**`
3. **Сохраните**

После этого Railway будет деплоить автоматически при любых изменениях, как Vercel.

### Вариант 2: Использовать принудительный редеплой (Когда нужно)

Если Watched Paths оставить, можно использовать принудительный редеплой когда нужно:

```bash
# Быстрый редеплой через CLI
railway redeploy --service "@make-bot/backend" --yes
```

## 📝 Создать алиас для быстрого редеплоя

Можно добавить в `package.json` скрипты для удобства:

```json
{
  "scripts": {
    "deploy:backend": "railway redeploy --service '@make-bot/backend' --yes",
    "deploy:bot": "railway redeploy --service '@make-bot/bot' --yes",
    "deploy:all": "npm run deploy:backend && npm run deploy:bot"
  }
}
```

Тогда можно просто:

```bash
npm run deploy:backend  # Передеплоить backend
npm run deploy:bot       # Передеплоить bot
npm run deploy:all       # Передеплоить все
```

## ✅ Итог

**Для задеплоя нового коммита, который Railway пропустил:**

### ✅ РЕКОМЕНДУЕТСЯ (Работает всегда):

```bash
# Задеплоить конкретный коммит
git checkout <commit-hash>
railway up --service "@make-bot/backend" --detach
git checkout main
```

### ❌ НЕ РАБОТАЕТ с Watched Paths:

- `railway redeploy` - передеплоит старый коммит, не новый
- Пустой коммит + push - Railway пропустит, если не в Watched Paths
- Redeploy через Dashboard - передеплоит старый коммит

### ✅ Постоянное решение:

Убрать Watched Paths, чтобы Railway автоматически деплоил все коммиты, как Vercel.

**Вывод:** Для задеплоя нового коммита используйте `git checkout <commit> && railway up`!

## 🔗 Связанные документы

- [RAILWAY_BACKEND_WATCHED_PATHS_FIX.md](./RAILWAY_BACKEND_WATCHED_PATHS_FIX.md) - Как убрать Watched Paths
- [RAILWAY_SKIP_DEPLOYMENT_FIX.md](./RAILWAY_SKIP_DEPLOYMENT_FIX.md) - Общее решение проблемы
