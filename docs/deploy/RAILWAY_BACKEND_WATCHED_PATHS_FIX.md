# 🔧 Railway Backend не деплоится - "watched paths not modified"

## Проблема

Backend сервис (`@make-bot/backend`) не деплоится автоматически, показывая:
```
✅ Successful - No deployment needed - watched paths not modified
```

В то время как Bot сервис (`@make-bot/bot`) деплоится нормально.

## 🔍 Причина

У backend сервиса настроены **"Watched Paths"** (отслеживаемые пути), которые ограничивают автоматический деплой только изменениями в определенных директориях (например, `/backend/**`).

Когда коммит не изменяет файлы в этих путях, Railway пропускает деплой для экономии ресурсов.

## ✅ Решение

### Вариант 1: Убрать ограничение на Watched Paths (РЕКОМЕНДУЕТСЯ)

**Через Railway Dashboard:**

1. **Откройте Railway Dashboard** → ваш проект → `@make-bot/backend`
2. **Перейдите в Settings** → **Deploy** (или **Build & Deploy**)
3. **Найдите раздел "Watched Paths"** или **"Build Triggers"**
4. **Удалите** все паттерны или **очистите** поле
5. **Или установите паттерн на корень:** `/**` (отслеживать все изменения)
6. **Сохраните изменения**

**После этого:**
- Railway будет деплоить при **любых** изменениях в репозитории
- Backend будет обновляться так же, как Bot

### Вариант 2: Расширить Watched Paths

Если хотите оставить оптимизацию, но включить больше путей:

1. **Railway Dashboard** → `@make-bot/backend` → **Settings** → **Deploy**
2. **Найдите "Watched Paths"**
3. **Добавьте паттерны:**
   ```
   /backend/**
   /frontend/**
   /railway.json
   /package.json
   /package-lock.json
   ```
4. **Сохраните**

**Это будет деплоить при изменениях в:**
- Backend коде
- Frontend коде (так как это монолитный деплой)
- Конфигурационных файлах

### Вариант 3: Использовать принудительный редеплой (временное решение)

Пока проблема не решена, можно использовать принудительный редеплой:

**Через Railway CLI:**
```bash
railway redeploy --service "@make-bot/backend" --yes
```

**Через Railway Dashboard:**
1. **Deployments** → последний успешный деплой
2. **Три точки (⋮)** → **Redeploy**

## 🎯 Пошаговая инструкция (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Открыть настройки Backend сервиса

1. **Railway Dashboard** → ваш проект
2. **Найдите сервис `@make-bot/backend`**
3. **Нажмите на него**

### Шаг 2: Найти настройки Deploy

1. **Перейдите в Settings** (вкладка вверху)
2. **Найдите раздел "Deploy"** или **"Build & Deploy"**
3. **Прокрутите вниз** до раздела **"Watched Paths"** или **"Build Triggers"**

### Шаг 3: Убрать ограничения

**Если видите поле "Watched Paths":**
- **Очистите** поле (удалите все паттерны)
- **Или** установите `/**` (отслеживать все)

**Если видите переключатель "Watch Paths":**
- **Выключите** его (disable)

**Если не видите этих настроек:**
- Возможно, они находятся в другом месте
- Попробуйте **Settings** → **Source** → проверьте там
- Или **Settings** → **Service Settings** → **Build & Deploy**

### Шаг 4: Сохранить и проверить

1. **Нажмите "Save"** или **"Update"**
2. **Создайте тестовый коммит:**
   ```bash
   echo "// Test: $(date)" >> backend/src/index.ts
   git add backend/src/index.ts
   git commit -m "test: verify backend auto-deploy"
   git push origin main
   ```
3. **Проверьте GitHub** → ваш репозиторий → вкладка **Checks**
4. **Должен появиться:** `Make CRM - @make-bot/backend: Pending - Railway is deploying the service`

## 🔍 Альтернативные места настройки

Если не нашли "Watched Paths" в Settings → Deploy, проверьте:

### 1. Settings → Source
- Может быть настройка "Watch Paths" или "Build Triggers"

### 2. Settings → Service Settings
- Раздел "Build & Deploy"
- Может быть переключатель "Enable watched paths"

### 3. Через Railway CLI (если доступно)

```bash
# Проверить текущие настройки
railway service

# Или через Railway API (если доступно)
```

## ⚠️ Важные замечания

1. **Убрав Watched Paths**, Railway будет деплоить при **любых** изменениях (даже в `docs/`)
2. Это может быть избыточно, но гарантирует, что деплой не пропустится
3. **Bot сервис работает нормально**, потому что у него нет ограничений на Watched Paths
4. **Backend сервис** имеет ограничения, поэтому пропускает деплои

## 🚀 Быстрое решение

1. **Railway Dashboard** → `@make-bot/backend` → **Settings** → **Deploy**
2. **Найдите "Watched Paths"** → **очистите** или установите `/**`
3. **Сохраните**
4. **Создайте тестовый коммит** и запушьте
5. **Проверьте**, что деплой запустился

## 📊 Сравнение сервисов

| Сервис | Watched Paths | Статус деплоя |
|--------|--------------|---------------|
| `@make-bot/bot` | ❌ Нет ограничений | ✅ Деплоится всегда |
| `@make-bot/backend` | ✅ Есть ограничения | ❌ Пропускает деплои |

**Решение:** Убрать ограничения у backend сервиса, чтобы он работал как bot сервис.

## ✅ Чеклист

- [ ] Открыл Railway Dashboard → `@make-bot/backend` → Settings
- [ ] Нашел раздел "Watched Paths" или "Build Triggers"
- [ ] Очистил паттерны или установил `/**`
- [ ] Сохранил изменения
- [ ] Создал тестовый коммит
- [ ] Проверил, что деплой запустился в GitHub Checks
- [ ] Убедился, что backend теперь деплоится как bot

## 🔗 Дополнительные ресурсы

- [Railway Documentation - Build Triggers](https://docs.railway.app/deploy/builds#build-triggers)
- [Railway Documentation - Service Settings](https://docs.railway.app/reference/service-settings)


