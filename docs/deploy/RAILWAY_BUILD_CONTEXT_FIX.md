# 🔧 Исправление проблем с контекстом сборки на Railway

## Проблема

При деплое на Railway возникают ошибки:
- `ERROR: package.json not found! Build context must be repository root.`
- `at require (node:internal/modules/helpers:177:18)`
- Frontend и Bot не запускаются

## Причина

Railway может использовать неправильный контекст сборки. Dockerfile ожидает, что сборка будет запущена из **корня репозитория**, но Railway может использовать папку сервиса (например, `frontend/` или `bot/`) как контекст сборки.

## ✅ Решение

### Шаг 1: Настройка сервисов в Railway

Для каждого сервиса (Frontend, Bot, Backend) нужно убедиться, что:

1. **Root Directory** должен быть **пустым** или установлен в **`.`** (корень репозитория)
2. **Dockerfile Path** должен указывать на правильный путь относительно корня:
   - Frontend: `frontend/Dockerfile`
   - Bot: `bot/Dockerfile`
   - Backend: `backend/Dockerfile`

### Шаг 2: Настройка через Railway UI

1. Откройте ваш проект в Railway
2. Для каждого сервиса перейдите в **Settings** → **Service Settings**
3. Найдите раздел **Build & Deploy**
4. Убедитесь, что:
   - **Root Directory:** пусто (или `.`)
   - **Dockerfile Path:** указан правильно (например, `frontend/Dockerfile`)
   - **Build Command:** оставьте пустым (Dockerfile сам соберет)
   - **Start Command:** оставьте пустым (Dockerfile сам запустит)

### Шаг 3: Проверка railway.json файлов

Убедитесь, что файлы `railway.json` существуют и настроены правильно:

**frontend/railway.json:**
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "frontend/Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

**bot/railway.json:**
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "bot/Dockerfile"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

**railway.json (для backend):**
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### Шаг 4: Пересоздание сервисов (если проблема не решена)

Если проблема сохраняется, попробуйте пересоздать сервисы:

1. **Удалите существующие сервисы** в Railway (или создайте новые)
2. **Создайте новый сервис** из GitHub репозитория
3. При создании:
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий
   - **НЕ устанавливайте Root Directory** (оставьте пустым)
   - Railway автоматически обнаружит `railway.json` файлы

### Шаг 5: Проверка логов сборки

После настройки проверьте логи сборки:

1. Откройте сервис в Railway
2. Перейдите в **Deployments** → выберите последний деплой
3. Откройте **Build Logs**
4. Убедитесь, что:
   - ✅ `package.json` найден
   - ✅ `npm install` выполнен успешно
   - ✅ Build прошел успешно
   - ✅ Нет ошибок с путями

## 🔍 Troubleshooting

### Ошибка: "package.json not found"

**Причина:** Railway использует неправильный контекст сборки.

**Решение:**
1. Проверьте Root Directory в настройках сервиса (должен быть пустым)
2. Убедитесь, что Dockerfile Path указан правильно относительно корня
3. Пересоздайте сервис с правильными настройками

### Ошибка: "Cannot find module"

**Причина:** Зависимости не установились или пути неправильные.

**Решение:**
1. Проверьте логи сборки - найдите этап `npm install`
2. Убедитесь, что `package-lock.json` существует в корне репозитория
3. Проверьте, что workspace структура правильная

### Ошибка: "dist/index.js not found"

**Причина:** Build не выполнился или выполнился с ошибками.

**Решение:**
1. Проверьте логи сборки на этапе `npm run build`
2. Убедитесь, что TypeScript компилируется без ошибок
3. Проверьте, что `tsconfig.json` существует и настроен правильно

### Frontend/Bot не запускается после успешной сборки

**Причина:** Проблемы с runtime или переменными окружения.

**Решение:**
1. Проверьте переменные окружения в Railway
2. Проверьте логи деплоя (Deploy Logs)
3. Убедитесь, что порты настроены правильно

## 📋 Чеклист настройки Railway

Для каждого сервиса (Frontend, Bot, Backend):

- [ ] Root Directory пустой (или `.`)
- [ ] Dockerfile Path указан правильно (например, `frontend/Dockerfile`)
- [ ] `railway.json` файл существует и настроен
- [ ] Build проходит успешно (проверьте логи)
- [ ] Все необходимые переменные окружения установлены
- [ ] Сервис запускается и отвечает на запросы

## 🎯 Ожидаемый результат

После правильной настройки:

1. ✅ Build проходит успешно без ошибок с путями
2. ✅ Все зависимости устанавливаются корректно
3. ✅ TypeScript компилируется без ошибок
4. ✅ Сервисы запускаются и работают
5. ✅ Логи показывают успешный запуск

## 📞 Дополнительная помощь

Если проблема не решена:

1. Проверьте полные логи сборки и деплоя в Railway
2. Убедитесь, что все файлы закоммичены в Git
3. Попробуйте пересоздать сервисы с нуля
4. Проверьте документацию Railway: https://docs.railway.app/deploy/builds

## ⚠️ Важно

**Все Dockerfile в этом проекте ожидают, что контекст сборки будет корень репозитория**, потому что проект использует npm workspaces и требует доступа к корневым файлам (`package.json`, `tsconfig.json`, `package-lock.json`).

Если Railway использует другую директорию как контекст сборки, сборка не будет работать правильно.

