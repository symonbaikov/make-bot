# 🚀 План запуска проекта

## ✅ Текущий статус

- ✅ Все .env файлы созданы и настроены
- ✅ Frontend зависимости установлены
- ❌ Backend зависимости не установлены
- ❌ Bot зависимости не установлены
- ❌ PostgreSQL не запущен
- ❌ Миграции не применены

---

## 📋 Пошаговый план

### Шаг 1: Установка зависимостей

```bash
# Установить зависимости для Backend
cd backend
npm install

# Установить зависимости для Bot
cd ../bot
npm install

# Frontend зависимости уже установлены ✅
```

---

### Шаг 2: Запуск PostgreSQL

```bash
# Вернуться в корень проекта
cd /Users/symonbaikov/make-bot

# Запустить PostgreSQL через Docker Compose
docker-compose up -d

# Проверить статус
docker-compose ps
```

**Ожидаемый результат:**

```
NAME                  STATUS
make-bot-postgres     Up
```

---

### Шаг 3: Настройка базы данных

```bash
cd backend

# 1. Сгенерировать Prisma Client
npm run db:generate

# 2. Создать и применить миграции
npm run db:migrate

# 3. Засеять базу данных (создать тестового админа)
npm run db:seed
```

**Что создается:**

- Таблицы: `users`, `sessions`, `actions`, `web_users`
- Тестовый админ:
  - Email: `admin@example.com`
  - Password: `admin123`

---

### Шаг 4: Запуск всех сервисов

#### Терминал 1: Backend API

```bash
cd backend
npm run dev
```

**Ожидаемый результат:**

```
Server running on http://localhost:3000
Environment: development
```

#### Терминал 2: Telegram Bot

```bash
cd bot
npm run dev
```

**Ожидаемый результат:**

```
Bot started successfully
Backend API health check: OK
```

#### Терминал 3: Frontend (уже запущен)

```bash
cd frontend
npm run dev
```

**Ожидаемый результат:**

```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5173/
```

---

### Шаг 5: Тестирование

#### 5.1 Проверить Backend API

```bash
curl http://localhost:3000/health
```

**Ожидаемый ответ:**

```json
{ "status": "ok", "timestamp": "2024-..." }
```

#### 5.2 Проверить Frontend

1. Откройте http://localhost:5173 в браузере
2. Должна открыться страница логина
3. Войдите с тестовыми данными:
   - **Email:** `admin@example.com`
   - **Password:** `admin123`

#### 5.3 Проверить Telegram Bot

1. Найдите вашего бота в Telegram (по username из `TELEGRAM_BOT_USERNAME`)
2. Отправьте `/start`
3. Бот должен ответить

---

### Шаг 6: Настройка Make (после тестирования)

1. Создайте аккаунт в Make (https://www.make.com)
2. Подключите PostgreSQL в Make
3. Создайте сценарии согласно `docs/MAKE_SETUP_GUIDE.md`
4. Добавьте webhook URL в `backend/.env`:
   ```
   MAKE_WEBHOOK_URL="https://hook.us1.make.com/your-webhook-url"
   ```
5. Перезапустите Backend API

---

## ✅ Checklist

- [ ] Backend зависимости установлены
- [ ] Bot зависимости установлены
- [ ] PostgreSQL запущен
- [ ] Prisma Client сгенерирован
- [ ] Миграции применены
- [ ] База данных засеяна
- [ ] Backend API запущен на порту 3000
- [ ] Telegram Bot запущен и отвечает
- [ ] Frontend открывается в браузере
- [ ] Можно войти в админ-панель
- [ ] Make сценарии настроены (опционально)

---

## 🆘 Если что-то не работает

### PostgreSQL не запускается

```bash
# Проверить логи
docker-compose logs postgres

# Пересоздать контейнер
docker-compose down
docker-compose up -d
```

### Миграции не применяются

```bash
# Проверить подключение к БД
cd backend
npm run db:studio

# Применить миграции заново
npm run db:migrate
```

### Backend не запускается

```bash
# Проверить .env файл
cat backend/.env

# Проверить, что все переменные заполнены
```

### Bot не отвечает

```bash
# Проверить токен
cat bot/.env | grep TELEGRAM_BOT_TOKEN

# Проверить подключение к Backend
curl http://localhost:3000/health
```

---

## 🎯 Готово!

После выполнения всех шагов у вас будет полностью рабочая система! 🚀
