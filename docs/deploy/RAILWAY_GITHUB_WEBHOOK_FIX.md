# 🔧 Railway не получает коммиты из GitHub - Полное решение

## Проблема

Railway не видит новые коммиты из GitHub, хотя Vercel их видит. Деплои не запускаются автоматически.

## 🔍 Диагностика

### Шаг 1: Проверить, что коммиты запушены

```bash
# Проверить последние коммиты
git log --oneline -5

# Проверить, что все запушено
git log origin/main..HEAD --oneline
# Если пусто - все запушено ✅
```

### Шаг 2: Проверить настройки Source в Railway

1. **Railway Dashboard** → ваш проект → `@make-bot/backend`
2. **Settings** → **Source**
3. Проверьте:
   - ✅ **Repository:** `symonbaikov/make-bot`
   - ✅ **Branch:** `main` (или ваша рабочая ветка)
   - ✅ **Root Directory:** пусто или `.` (корень репозитория)
   - ✅ **Connected:** должно быть "Connected" (зеленая галочка)

### Шаг 3: Проверить GitHub Webhook (КРИТИЧНО!)

1. **GitHub** → ваш репозиторий `symonbaikov/make-bot`
2. **Settings** → **Webhooks**
3. Найдите webhook для Railway (обычно URL содержит `railway.app` или `api.railway.app`)
4. Проверьте:
   - ✅ **Status:** должен быть зеленый (Active)
   - ✅ **Recent Deliveries:** нажмите на webhook и проверьте последние доставки
   - ✅ **Последняя доставка:** должна быть недавней (после вашего последнего push)

**Если webhook НЕ существует или показывает ошибки:**

- Это и есть причина проблемы!
- Railway не получает уведомления о новых коммитах

## ✅ Решения

### Решение 1: Переподключить репозиторий (РЕКОМЕНДУЕТСЯ)

**Это самое надежное решение:**

1. **Railway Dashboard** → `@make-bot/backend` → **Settings** → **Source**
2. **Нажмите "Disconnect"** (отключить репозиторий)
3. **Подтвердите отключение**
4. **Нажмите "Connect"** или **"+ Connect Repository"**
5. Выберите:
   - **Repository:** `symonbaikov/make-bot`
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым (или `.`)
6. **Сохраните**

**После переподключения:**

- Railway автоматически создаст новый webhook в GitHub
- Railway запустит деплой текущего коммита
- Все последующие коммиты будут автоматически деплоиться

### Решение 2: Проверить и исправить GitHub Webhook вручную

Если переподключение не помогло:

1. **GitHub** → репозиторий → **Settings** → **Webhooks**
2. Найдите webhook для Railway
3. **Проверьте URL:** должен быть типа `https://api.railway.app/webhook/...`
4. **Проверьте события (Events):**
   - Должны быть выбраны: `push`, `pull_request` (или "Just the push event")
5. **Проверьте последние доставки:**
   - Нажмите на webhook
   - Прокрутите вниз до "Recent Deliveries"
   - Проверьте последние доставки:
     - ✅ Зеленые (200) - webhook работает
     - ❌ Красные (404, 500) - webhook сломан

**Если webhook показывает ошибки:**

- Нажмите на доставку с ошибкой
- Посмотрите "Response" - там будет причина ошибки
- Обычно это означает, что Railway не может обработать webhook

**Если webhook не существует:**

- Переподключите репозиторий (Решение 1)
- Или создайте вручную (см. ниже)

### Решение 3: Создать GitHub Webhook вручную (если нужно)

**Обычно это НЕ нужно** - Railway создает webhook автоматически при подключении репозитория.

Но если нужно создать вручную:

1. **GitHub** → репозиторий → **Settings** → **Webhooks** → **Add webhook**
2. **Payload URL:** нужно получить из Railway
   - Откройте Railway Dashboard → проект → **Settings** → **Deployments**
   - Или используйте формат: `https://api.railway.app/webhook/<project-id>`
3. **Content type:** `application/json`
4. **Events:** выберите "Just the push event"
5. **Active:** ✅
6. **Add webhook**

**⚠️ Внимание:** Лучше использовать переподключение репозитория (Решение 1), так как Railway автоматически настроит правильный URL.

### Решение 4: Проверить права доступа Railway к репозиторию

1. **GitHub** → ваш профиль → **Settings** → **Applications** → **Authorized OAuth Apps**
2. Найдите **Railway**
3. Проверьте:
   - ✅ Railway имеет доступ к репозиторию
   - ✅ Права: `repo` (доступ к репозиторию)

**Если Railway нет в списке:**

- Переподключите репозиторий в Railway (Решение 1)
- Railway запросит права доступа автоматически

### Решение 5: Использовать Railway CLI для принудительного деплоя

Пока проблема не решена, можно деплоить вручную:

```bash
# Переключиться на нужный коммит
git checkout <commit-hash>

# Задеплоить
railway up --detach

# Вернуться на main
git checkout main
```

Или использовать redeploy:

```bash
# Передеплоить последний успешный деплой
railway redeploy --service "@make-bot/backend" --yes
```

## 🎯 Пошаговая инструкция (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Переподключить репозиторий

1. **Railway Dashboard** → `@make-bot/backend` → **Settings** → **Source**
2. **Disconnect** → подтвердите
3. **Connect** → выберите `symonbaikov/make-bot` → `main` → сохраните

### Шаг 2: Проверить, что webhook создан

1. **GitHub** → репозиторий → **Settings** → **Webhooks**
2. Должен появиться новый webhook для Railway
3. Проверьте статус - должен быть зеленый (Active)

### Шаг 3: Протестировать

```bash
# Создать тестовый коммит
echo "// Test: $(date)" >> backend/src/index.ts
git add backend/src/index.ts
git commit -m "test: trigger railway deployment"
git push origin main
```

**Проверьте Railway Dashboard:**

- Должен появиться новый деплой через 10-30 секунд после push
- Если не появился - проверьте логи webhook в GitHub

### Шаг 4: Проверить логи webhook в GitHub

1. **GitHub** → репозиторий → **Settings** → **Webhooks**
2. Нажмите на webhook Railway
3. Прокрутите до "Recent Deliveries"
4. Найдите последнюю доставку (после вашего push)
5. Проверьте:
   - ✅ **Status:** 200 (зеленый) - webhook работает
   - ❌ **Status:** 404, 500 (красный) - webhook сломан

**Если статус красный:**

- Нажмите на доставку
- Посмотрите "Response" - там будет ошибка
- Обычно это означает проблему на стороне Railway

## 🔍 Частые причины проблемы

### 1. Webhook удален или сломан

- **Решение:** Переподключить репозиторий (Решение 1)

### 2. Railway потерял доступ к репозиторию

- **Решение:** Проверить права доступа (Решение 4)

### 3. Неправильная ветка в настройках

- **Решение:** Проверить Settings → Source → Branch

### 4. Репозиторий переименован или перемещен

- **Решение:** Переподключить репозиторий с новым именем

### 5. Railway временно недоступен

- **Решение:** Проверить [Railway Status](https://status.railway.app/)

## ⚠️ Важные замечания

1. **Vercel работает, а Railway нет** - это означает, что проблема именно в настройках Railway, а не в GitHub
2. **Webhook - это ключевой элемент** - без него Railway не узнает о новых коммитах
3. **Переподключение репозитория** - самый надежный способ исправить проблему
4. **После переподключения** Railway автоматически создаст новый webhook и запустит деплой

## 🚀 Быстрое решение (если ничего не помогло)

1. **Railway Dashboard** → `@make-bot/backend` → **Settings** → **Source**
2. **Disconnect** → подтвердите
3. **Connect** → `symonbaikov/make-bot` → `main` → сохраните
4. **Проверьте GitHub Webhooks** - должен появиться новый webhook
5. **Создайте тестовый коммит** и запушьте
6. **Проверьте Railway** - должен появиться новый деплой

## 📞 Дополнительная помощь

Если проблема не решается:

1. Проверьте [Railway Status](https://status.railway.app/)
2. Проверьте [Railway Discord](https://discord.gg/railway)
3. Проверьте логи Railway на наличие ошибок
4. Убедитесь, что у вас есть доступ к репозиторию в Railway

## ✅ Чеклист решения проблемы

- [ ] Проверил, что коммиты запушены в GitHub
- [ ] Проверил настройки Source в Railway (Repository, Branch)
- [ ] Переподключил репозиторий в Railway
- [ ] Проверил, что webhook создан в GitHub
- [ ] Проверил статус webhook (должен быть Active)
- [ ] Проверил последние доставки webhook (должны быть 200)
- [ ] Создал тестовый коммит и проверил, что деплой запустился
- [ ] Проверил права доступа Railway к репозиторию
