# Диагностика проблемы: Telegram не отправляет запросы на webhook

## Проблема

Бот запущен, webhook настроен, но запросы от Telegram не доходят до сервера. В логах нет записей `📥 Webhook request received`.

## Что было добавлено для диагностики

1. **Логирование всех входящих HTTP запросов** - теперь видно все запросы, даже не на webhook путь
2. **Периодическая проверка статуса webhook** - каждую минуту проверяется статус и логируются ошибки
3. **Автоматический сброс webhook** - при обнаружении ошибок webhook автоматически сбрасывается
4. **Расширенная информация в /webhook-test** - полная информация о статусе webhook
5. **Обработчик необработанных путей** - логирует все запросы на неизвестные пути

## Шаги диагностики

### 1. Проверьте статус webhook в Telegram

Откройте в браузере:
```
https://make-botbot-production.up.railway.app/webhook-test
```

Проверьте:
- `last_error_message` - если есть ошибка, это причина проблемы
- `last_error_date` - когда произошла последняя ошибка
- `pending_update_count` - количество ожидающих обновлений
- `url` - правильный ли URL установлен

### 2. Проверьте логи на наличие ошибок webhook

В логах ищите:
- `⚠️ Webhook error detected from Telegram` - ошибки от Telegram
- `⚠️ Pending updates detected` - ожидающие обновления
- `📥 Incoming HTTP request` - все входящие HTTP запросы

### 3. Проверьте, что Telegram может достучаться до сервера

Проверьте health endpoint:
```bash
curl https://make-botbot-production.up.railway.app/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "mode": "webhook",
  "webhookPath": "/webhook"
}
```

### 4. Проверьте логи на наличие входящих запросов

После отправки команды `/start` в боте, проверьте логи на наличие:
- `📥 Incoming HTTP request` - должен появиться при любом HTTP запросе
- `📥 Webhook request received` - должен появиться при webhook запросе от Telegram

### 5. Принудительно сбросьте webhook

Откройте в браузере:
```
https://make-botbot-production.up.railway.app/webhook-test?reset=true
```

Или отправьте POST запрос:
```bash
curl -X POST https://make-botbot-production.up.railway.app/webhook-setup
```

## Возможные причины и решения

### Причина 1: Telegram не может достучаться до сервера

**Симптомы:**
- В логах нет `📥 Incoming HTTP request` при отправке команды
- В `/webhook-test` есть `last_error_message` типа "Connection refused" или "Timeout"

**Решение:**
- Проверьте, что сервер доступен из интернета
- Проверьте firewall настройки Railway
- Проверьте, что используется HTTPS (Telegram требует HTTPS)

### Причина 2: Неправильный URL webhook

**Симптомы:**
- В `/webhook-test` `url` не совпадает с `expectedUrl`
- В логах есть ошибки при настройке webhook

**Решение:**
- Проверьте переменную окружения `TELEGRAM_WEBHOOK_URL`
- Убедитесь, что URL использует HTTPS
- Убедитесь, что URL включает путь (не просто домен)

### Причина 3: SSL сертификат недействителен

**Симптомы:**
- В `/webhook-test` есть `last_error_message` про SSL
- Telegram не может установить соединение

**Решение:**
- Railway автоматически предоставляет SSL сертификаты
- Проверьте, что домен правильно настроен
- Проверьте, что сертификат действителен

### Причина 4: Webhook был удален или изменен

**Симптомы:**
- В `/webhook-test` `url` пустой или другой
- В логах нет ошибок, но запросы не приходят

**Решение:**
- Принудительно сбросьте webhook через `/webhook-setup`
- Проверьте, что никто другой не использует этот бот токен

### Причина 5: Railway блокирует запросы

**Симптомы:**
- В логах нет `📥 Incoming HTTP request` вообще
- Health endpoint работает, но webhook запросы не приходят

**Решение:**
- Проверьте настройки Railway
- Убедитесь, что порт правильно настроен
- Проверьте, что нет ограничений на входящие запросы

## Что проверить в логах после перезапуска

1. **При старте бота:**
   ```
   ✅ Webhook configured
   ✅ Webhook status verified
   ```

2. **При отправке команды /start:**
   ```
   📥 Incoming HTTP request (должен появиться)
   📥 Webhook request received (должен появиться)
   📥 /start command received in bot.start handler (должен появиться)
   ```

3. **Каждую минуту (периодическая проверка):**
   ```
   (ничего, если все хорошо)
   ⚠️ Webhook error detected from Telegram (если есть ошибка)
   ```

## Быстрая проверка

1. Откройте: `https://make-botbot-production.up.railway.app/webhook-test`
2. Проверьте `last_error_message` - если есть, это причина
3. Если ошибки нет, отправьте команду `/start` в боте
4. Проверьте логи на наличие `📥 Incoming HTTP request`
5. Если запросов нет - проблема с доступностью сервера
6. Если запросы есть, но не обрабатываются - проблема с обработчиком

## Дополнительная информация

Все входящие HTTP запросы теперь логируются с полной информацией:
- Метод (GET/POST)
- Путь
- Заголовки
- Тело запроса (первые 500 символов)

Это поможет понять, что именно приходит на сервер и почему не обрабатывается.

