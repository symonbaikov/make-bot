# Диагностика проблем с ботом

## Проблема: Бот не отвечает на команду /start

### Симптомы
- В логах Railway видно `Pending updates detected { pendingUpdates: 1 }`
- Пользователь отправляет `/start`, но бот не отвечает
- Webhook настроен правильно, но обновления не обрабатываются

### Что было добавлено

1. **Детальное логирование**:
   - Логирование всех входящих webhook запросов
   - Логирование начала и завершения обработки обновлений
   - Логирование всех этапов обработки команды `/start`
   - Логирование ошибок с полным стеком вызовов

2. **Улучшенная обработка ошибок**:
   - Ошибки при обработке обновлений не прерывают работу webhook handler
   - Всегда возвращается 200 статус в Telegram (даже при ошибках)
   - Попытка отправить сообщение об ошибке пользователю

3. **Автоматическая очистка pending updates**:
   - При обнаружении более 5 pending updates автоматически очищаются
   - Периодическая проверка статуса webhook каждую минуту

### Как проверить логи

1. Откройте Railway Dashboard → Deploy Logs
2. Найдите записи с тегами:
   - `📥 Webhook request received` - входящий запрос от Telegram
   - `🔄 Processing update` - начало обработки обновления
   - `📥 /start command received` - получение команды /start
   - `🔄 Calling handleStart function` - вызов функции обработки
   - `✅ Welcome message sent successfully` - успешная отправка сообщения
   - `❌ Error` - любые ошибки

### Что проверить в логах

1. **Приходит ли webhook запрос?**
   - Ищите `📥 Webhook request received`
   - Проверьте `updateId`, `userId`, `chatId`

2. **Обрабатывается ли обновление?**
   - Ищите `🔄 Processing update`
   - Проверьте `updateType`, `messageText`

3. **Вызывается ли обработчик /start?**
   - Ищите `📥 /start command received in bot.start handler`
   - Проверьте `hasSession`, `commandText`

4. **Отправляется ли сообщение?**
   - Ищите `✅ Welcome message sent successfully`
   - Проверьте `messageId`, `chatId`

5. **Есть ли ошибки?**
   - Ищите `❌ Error` или `❌ Failed`
   - Проверьте `error`, `stack`, `errorType`

### Возможные проблемы и решения

#### Проблема 1: Webhook не получает запросы
**Симптомы**: Нет логов `📥 Webhook request received`

**Решение**:
1. Проверьте `TELEGRAM_WEBHOOK_URL` в переменных окружения
2. Убедитесь, что URL использует HTTPS
3. Проверьте, что путь правильный (например, `/webhook`)
4. Вызовите `/webhook-test?reset=true` для переустановки webhook

#### Проблема 2: Обновление получается, но не обрабатывается
**Симптомы**: Есть `📥 Webhook request received`, но нет `🔄 Processing update`

**Решение**:
1. Проверьте, что `req.body` содержит `update_id`
2. Проверьте формат данных от Telegram
3. Проверьте логи на наличие ошибок валидации

#### Проблема 3: Обработчик /start не вызывается
**Симптомы**: Есть `🔄 Processing update`, но нет `📥 /start command received`

**Решение**:
1. Проверьте, что команда правильно распознается (есть `entities` с типом `bot_command`)
2. Проверьте, что `message.text` содержит `/start`
3. Проверьте регистрацию обработчика `bot.start`

#### Проблема 4: Сообщение не отправляется
**Симптомы**: Есть `🔄 Calling handleStart function`, но нет `✅ Welcome message sent successfully`

**Решение**:
1. Проверьте логи на ошибки отправки (`❌ Failed to send welcome message`)
2. Проверьте `errorCode` и `errorDescription` в логах
3. Возможные причины:
   - Неверный токен бота
   - Бот заблокирован пользователем
   - Проблемы с Telegram API
   - Превышен лимит отправки сообщений

#### Проблема 5: Pending updates накапливаются
**Симптомы**: `Pending updates detected { pendingUpdates: N }` где N > 0

**Решение**:
1. Автоматически очищается при N > 5
2. Можно вручную очистить через `/webhook-test?reset=true`
3. Проверьте логи на ошибки обработки обновлений

### Ручная диагностика

1. **Проверка webhook статуса**:
   ```bash
   curl https://make-botbot-production.up.railway.app/webhook-test
   ```

2. **Переустановка webhook**:
   ```bash
   curl https://make-botbot-production.up.railway.app/webhook-test?reset=true
   ```

3. **Проверка здоровья бота**:
   ```bash
   curl https://make-botbot-production.up.railway.app/health
   ```

### Следующие шаги

После деплоя изменений:

1. Отправьте команду `/start` боту
2. Проверьте логи в Railway Dashboard
3. Найдите все записи, связанные с вашим `updateId`
4. Проверьте, на каком этапе происходит ошибка
5. Используйте информацию из логов для дальнейшей диагностики

### Контакты для поддержки

Если проблема не решается:
1. Соберите все логи с момента отправки `/start`
2. Укажите `updateId` из логов
3. Укажите `userId` и `chatId`
4. Опишите, на каком этапе происходит ошибка

