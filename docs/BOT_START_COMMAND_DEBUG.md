# Диагностика проблемы с командой /start

## Что было изменено

1. **Исправлено форматирование** - добавлен пробел после эмодзи: `✋ Вітаю` (вместо `✋Вітаю`)
2. **Добавлено расширенное логирование** для отладки:
   - Логирование перед отправкой сообщения
   - Детальная информация об ошибках (код ошибки, описание)
   - Логирование на каждом этапе обработки команды

## Возможные причины проблемы

### 1. Webhook не настроен или не работает

**Проверка:**
- Проверьте логи при старте бота - должно быть сообщение `✅ Webhook configured`
- Проверьте статус webhook через endpoint: `GET /webhook-test`
- Убедитесь, что `TELEGRAM_WEBHOOK_URL` правильно настроен

**Решение:**
```bash
# Проверьте webhook статус
curl https://your-bot-url/webhook-test

# Принудительно переустановите webhook
curl -X POST https://your-bot-url/webhook-setup
```

### 2. Запросы не доходят до бота

**Проверка в логах:**
Ищите строки:
- `📥 Webhook request received` - должен появляться при каждом запросе от Telegram
- `📥 /start command received in bot.start handler` - должен появляться при команде /start
- `Start command received` - должен появляться в handleStart

**Если этих логов нет:**
- Webhook не настроен правильно
- Telegram не может достучаться до вашего сервера
- Проблема с маршрутизацией запросов

### 3. Ошибка при отправке сообщения

**Проверка в логах:**
Ищите строки:
- `❌ Failed to send welcome message` - ошибка при отправке
- `errorCode` и `errorDescription` в логах - код ошибки от Telegram API

**Возможные причины:**
- Бот заблокирован пользователем
- Проблема с правами бота
- Превышен лимит отправки сообщений
- Неправильный формат сообщения (редко)

### 4. Проблема с middleware сессий

**Проверка в логах:**
- `hasSession: true` должно быть в логах
- Если `hasSession: false` - проблема с middleware

### 5. Таймаут обработки

**Проверка в логах:**
- `Update handling timeout after 10s` - запрос обрабатывается слишком долго
- Проверьте производительность бота и доступность backend API

## Что проверить в логах

После отправки команды `/start` в логах должны появиться следующие записи (в порядке появления):

1. **Webhook получен:**
   ```
   📥 Webhook request received
   ```

2. **Обработка обновления:**
   ```
   🔄 Processing update
   ```

3. **Команда получена в обработчике:**
   ```
   📥 /start command received in bot.start handler
   ```

4. **Команда получена в handleStart:**
   ```
   Start command received
   ```

5. **Сессия инициализирована:**
   ```
   Session initialized for data collection
   ```

6. **Попытка отправить сообщение:**
   ```
   Attempting to send reply
   ```

7. **Сообщение отправлено успешно:**
   ```
   ✅ Welcome message sent successfully
   ```

8. **Команда завершена:**
   ```
   ✅ /start command completed successfully
   ```

## Если сообщение не отправляется

Если вы видите логи до `Attempting to send reply`, но не видите `✅ Welcome message sent successfully`, значит проблема в отправке сообщения.

**Проверьте:**
1. Код ошибки в логах (`errorCode`)
2. Описание ошибки (`errorDescription`)
3. Stack trace для деталей

**Частые ошибки Telegram API:**
- `403` - Бот заблокирован пользователем
- `400` - Неправильный формат запроса
- `429` - Превышен лимит запросов (rate limit)
- `502` - Проблема на стороне Telegram

## Быстрая диагностика

1. **Проверьте, что бот запущен:**
   ```bash
   curl https://your-bot-url/health
   ```

2. **Проверьте webhook:**
   ```bash
   curl https://your-bot-url/webhook-test
   ```

3. **Проверьте логи в реальном времени:**
   - Отправьте команду `/start`
   - Сразу проверьте логи - должны появиться записи выше

4. **Проверьте, что запрос доходит:**
   - Если нет логов `📥 Webhook request received` - проблема с webhook
   - Если есть эти логи, но нет `📥 /start command received` - проблема с обработчиком

## Решение проблем

### Проблема: Нет логов вообще
**Решение:** Проверьте, что бот запущен и логирование работает

### Проблема: Есть `Webhook request received`, но нет `Start command received`
**Решение:** Проверьте регистрацию обработчика в `bot/src/index.ts`

### Проблема: Есть `Start command received`, но нет `Welcome message sent`
**Решение:** Проверьте код ошибки в логах и исправьте проблему с отправкой

### Проблема: Все логи есть, но сообщение не приходит пользователю
**Решение:** Проверьте, что бот не заблокирован пользователем, и что у бота есть права на отправку сообщений

## Дополнительная информация

После внесения изменений:
1. Перезапустите бота
2. Проверьте логи при старте
3. Отправьте команду `/start`
4. Проверьте логи - теперь должно быть больше информации для диагностики

Если проблема сохраняется, пришлите логи с момента отправки команды `/start` для дальнейшей диагностики.

