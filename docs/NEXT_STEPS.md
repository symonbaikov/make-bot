# Следующие шаги после настройки .env файлов

## ✅ Что уже сделано

- ✅ Все .env файлы созданы и настроены
- ✅ Переменные окружения добавлены

---

## 🚀 План действий

### Шаг 1: Установка зависимостей

```bash
# Установить зависимости для всех сервисов
cd /Users/symonbaikov/make-bot
npm install

# Backend зависимости
cd backend
npm install

# Bot зависимости
cd ../bot
npm install

# Frontend зависимости
cd ../frontend
npm install
```

---

### Шаг 2: Запуск PostgreSQL

```bash
# Запустить PostgreSQL через Docker Compose
cd /Users/symonbaikov/make-bot
docker-compose up -d

# Проверить, что PostgreSQL запущен
docker-compose ps
```

**Ожидаемый результат:** PostgreSQL должен быть в статусе "Up"

---

### Шаг 3: Настройка базы данных

```bash
cd backend

# 1. Сгенерировать Prisma Client
npm run db:generate

# 2. Применить миграции
npm run db:migrate

# 3. Засеять базу данных (создать тестового админа)
npm run db:seed
```

**Что создается:**
- Таблицы: `users`, `sessions`, `actions`, `web_users`
- Тестовый админ: `admin@example.com` / `admin123`

---

### Шаг 4: Запуск сервисов

#### 4.1 Запустить Backend API

```bash
cd backend
npm run dev
```

**Ожидаемый результат:**
```
Server running on http://localhost:3000
Environment: development
```

#### 4.2 Запустить Telegram Bot

```bash
cd bot
npm run dev
```

**Ожидаемый результат:**
```
Bot started successfully
Backend API health check: OK
```

#### 4.3 Запустить Frontend

```bash
cd frontend
npm run dev
```

**Ожидаемый результат:**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5173/
```

---

### Шаг 5: Тестирование базового функционала

#### 5.1 Проверить Backend API

```bash
# Health check
curl http://localhost:3000/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"2024-..."}
```

#### 5.2 Проверить Frontend

1. Откройте http://localhost:5173 в браузере
2. Должна открыться страница логина
3. Войдите с тестовыми данными:
   - Email: `admin@example.com`
   - Password: `admin123`

#### 5.3 Проверить Telegram Bot

1. Найдите вашего бота в Telegram (по username из `TELEGRAM_BOT_USERNAME`)
2. Отправьте `/start`
3. Бот должен ответить

---

### Шаг 6: Настройка Make сценариев

#### 6.1 Создать аккаунт в Make (если еще нет)

1. Перейдите на https://www.make.com
2. Создайте аккаунт или войдите

#### 6.2 Подключить PostgreSQL в Make

1. В Make перейдите в **Connections** → **Add a connection**
2. Выберите **PostgreSQL**
3. Заполните данные из `docker-compose.yml`:
   - Host: `localhost` (или ваш хост)
   - Port: `5432`
   - Database: `make_bot`
   - Username: `makebot`
   - Password: `makebot123`

#### 6.3 Создать сценарии в Make

Следуйте инструкциям в `docs/MAKE_SETUP_GUIDE.md`:

1. **Bot Webhook Handler** - для обработки email от бота
2. **PayPal Webhook Handler** - для обработки платежей
3. **Access Provisioning** - для предоставления доступа
4. **Nightly Checks** - для ночных проверок

#### 6.4 Добавить Make Webhook URL в Backend

После создания сценариев:

1. Скопируйте webhook URL из Make
2. Добавьте в `backend/.env`:
   ```
   MAKE_WEBHOOK_URL="https://hook.us1.make.com/your-webhook-url"
   ```
3. Перезапустите Backend API

---

### Шаг 7: Тестирование полного flow

#### 7.1 Тест: Создание сессии через админ-панель

1. Войдите в админ-панель (http://localhost:5173)
2. Перейдите в **Create Session**
3. Создайте тестовую сессию:
   - Plan: `STANDARD`
   - Amount: `99.99`
   - Currency: `USD`
4. Скопируйте ссылку на бота

#### 7.2 Тест: Telegram Bot Flow

1. Откройте ссылку на бота в Telegram
2. Отправьте email адрес
3. Проверьте, что данные сохранились в БД
4. Проверьте, что webhook отправлен в Make

#### 7.3 Тест: PayPal Payment (опционально)

1. Симулируйте PayPal webhook через Backend API
2. Проверьте обновление сессии в БД
3. Проверьте выполнение Make сценария

---

## ✅ Checklist готовности

- [ ] Зависимости установлены для всех сервисов
- [ ] PostgreSQL запущен и работает
- [ ] Миграции применены
- [ ] База данных засеяна (seed)
- [ ] Backend API запущен на порту 3000
- [ ] Telegram Bot запущен и отвечает
- [ ] Frontend запущен на порту 5173
- [ ] Админ-панель открывается и можно войти
- [ ] Make сценарии настроены
- [ ] Make webhook URL добавлен в Backend .env
- [ ] Полный flow протестирован

---

## 🆘 Troubleshooting

### Проблема: PostgreSQL не запускается

```bash
# Проверить логи
docker-compose logs postgres

# Пересоздать контейнер
docker-compose down
docker-compose up -d
```

### Проблема: Миграции не применяются

```bash
# Проверить подключение к БД
cd backend
npm run db:studio

# Применить миграции заново
npm run db:migrate
```

### Проблема: Backend не запускается

```bash
# Проверить .env файл
cat backend/.env

# Проверить логи
cd backend
npm run dev
```

### Проблема: Bot не отвечает

```bash
# Проверить токен в .env
cat bot/.env | grep TELEGRAM_BOT_TOKEN

# Проверить подключение к Backend
curl http://localhost:3000/health
```

### Проблема: Frontend не подключается к Backend

```bash
# Проверить VITE_API_URL
cat frontend/.env

# Убедиться, что Backend запущен
curl http://localhost:3000/health
```

---

## 📚 Полезные команды

### Просмотр логов

```bash
# Backend логи
cd backend && npm run dev

# Bot логи
cd bot && npm run dev

# PostgreSQL логи
docker-compose logs -f postgres
```

### Работа с базой данных

```bash
# Открыть Prisma Studio (GUI для БД)
cd backend
npm run db:studio

# Создать новую миграцию
npm run db:migrate

# Сбросить базу данных (ОСТОРОЖНО!)
npm run db:migrate reset
```

### Остановка сервисов

```bash
# Остановить все Docker контейнеры
docker-compose down

# Остановить Backend/Bot/Frontend
# Нажмите Ctrl+C в терминалах где они запущены
```

---

## 🎯 Итог

После выполнения всех шагов у вас будет:

1. ✅ Работающий Backend API
2. ✅ Работающий Telegram Bot
3. ✅ Работающая админ-панель
4. ✅ Настроенные Make сценарии
5. ✅ Полный автоматизированный flow

**Система готова к использованию!** 🚀

