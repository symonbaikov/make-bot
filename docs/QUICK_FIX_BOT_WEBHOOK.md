# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: Бот не отвечает

## Проблема

`TELEGRAM_WEBHOOK_URL` установлен неправильно:
```
https://make-botbot-production.up.railway.app/
```

**Отсутствует `/webhook` в конце!**

## Решение (СРОЧНО!)

### Шаг 1: Исправьте `TELEGRAM_WEBHOOK_URL`

В Railway для **Bot сервиса** (`@make-bot/bot`):

1. Откройте **Variables**
2. Найдите `TELEGRAM_WEBHOOK_URL`
3. Измените значение на:
   ```
   https://make-botbot-production.up.railway.app/webhook
   ```
   
   **Важно:** Добавьте `/webhook` в конец URL!

### Шаг 2: Проверьте другие переменные

Убедитесь, что установлены:
- `TELEGRAM_BOT_TOKEN` - токен бота
- `API_URL=https://make-botbackend-production.up.railway.app` - URL бэкенда
- `NODE_ENV=production` - для продакшена

### Шаг 3: Перезапустите бот

После изменения переменной Railway автоматически перезапустит бот.

### Шаг 4: Проверьте логи

В логах бота должно быть:
```
Starting bot in webhook mode: https://make-botbot-production.up.railway.app/webhook
Webhook server listening on port 8080
Webhook set to: https://make-botbot-production.up.railway.app/webhook
```

Если видите ошибку `Failed to set webhook`, проверьте:
- URL правильный и доступен
- Telegram может достучаться до вашего сервера

### Шаг 5: Проверьте доступность webhook

Откройте в браузере:
```
https://make-botbot-production.up.railway.app/health
```

Должно вернуть:
```json
{"status":"ok","mode":"webhook"}
```

## Альтернативное решение (если webhook не работает)

Если webhook все еще не работает, можно временно использовать polling:

1. Установите переменную:
   ```
   ALLOW_POLLING_IN_PRODUCTION=true
   ```

2. Удалите или закомментируйте `TELEGRAM_WEBHOOK_URL`

**Внимание:** Polling не рекомендуется для продакшена, используйте только для тестирования!

