# Email Integration Guide

## Обзор

Система поддерживает отправку email сообщений конкретным пользователям прямо из админ-панели. Эта функциональность позволяет администраторам отправлять персонализированные сообщения пользователям, которые предоставили свой email через Telegram бота.

## Установка

### 1. Применить миграцию базы данных

После добавления нового `ActionType` необходимо применить миграцию:

```bash
cd backend
npx prisma migrate dev --name add_email_sent_admin_action_type
```

Или если база данных уже настроена:

```bash
cd backend
npx prisma db push
```

### 2. Перегенерировать Prisma Client

```bash
cd backend
npx prisma generate
```

## Возможности

- ✅ Отправка email конкретным пользователям из списка платежей
- ✅ Использование email адреса, предоставленного пользователем через Telegram бота
- ✅ Логирование всех отправленных email в таблицу `actions`
- ✅ Валидация данных перед отправкой
- ✅ Поддержка HTML и plain text форматов

## Архитектура

### Backend

1. **EmailService** (`backend/src/services/email-service.ts`)
   - Метод `sendEmailToUser()` для отправки кастомных email
   - Использует nodemailer для отправки через SMTP
   - Поддерживает HTML и plain text форматы

2. **AdminController** (`backend/src/controllers/admin-controller.ts`)
   - Метод `sendEmail()` для обработки запросов на отправку email
   - Валидация входных данных
   - Логирование действий

3. **API Endpoint**
   - `POST /api/admin/payments/:id/send-email`
   - Требует аутентификации
   - Принимает `subject` и `body` в теле запроса

### Frontend

1. **SendEmailModal** (`frontend/src/components/SendEmailModal.tsx`)
   - Модальное окно с формой для отправки email
   - Валидация через React Hook Form + Zod
   - Отображение получателя email

2. **Payments Page** (`frontend/src/pages/Payments.tsx`)
   - Кнопка конверта рядом с каждым пользователем
   - Открывает модальное окно для отправки email

## Настройка SMTP

### Переменные окружения

Добавьте следующие переменные в файл `.env` в директории `backend/`:

```env
# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com
```

### Настройка для популярных провайдеров

#### Gmail

1. Включите двухфакторную аутентификацию в вашем Google аккаунте
2. Создайте App Password:
   - Перейдите в [Google Account Settings](https://myaccount.google.com/)
   - Security → 2-Step Verification → App passwords
   - Создайте новый пароль для "Mail"
3. Используйте следующие настройки:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-character-app-password
SMTP_FROM=your-email@gmail.com
```

#### Outlook / Microsoft 365

```env
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@outlook.com
SMTP_PASSWORD=your-password
SMTP_FROM=your-email@outlook.com
```

#### SendGrid

```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASSWORD=your-sendgrid-api-key
SMTP_FROM=noreply@yourdomain.com
```

#### Mailgun

```env
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=postmaster@yourdomain.mailgun.org
SMTP_PASSWORD=your-mailgun-password
SMTP_FROM=noreply@yourdomain.com
```

#### AWS SES

```env
SMTP_HOST=email-smtp.us-east-1.amazonaws.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-ses-smtp-username
SMTP_PASSWORD=your-ses-smtp-password
SMTP_FROM=noreply@yourdomain.com
```

### Режим разработки

Если SMTP не настроен, система автоматически использует console transport для разработки. Email будут логироваться в консоль вместо реальной отправки.

## Использование

### Отправка email из админ-панели

1. Откройте страницу "Платежі" (`/payments`)
2. Найдите пользователя, которому хотите отправить email
3. Нажмите на иконку конверта (📧) рядом с именем пользователя
4. Заполните форму:
   - **Тема листа** (обязательно, максимум 200 символов)
   - **Текст листа** (обязательно, максимум 10000 символов)
5. Нажмите "Відправити"

### API Использование

#### Запрос

```http
POST /api/admin/payments/:id/send-email
Authorization: Bearer <jwt-token>
Content-Type: application/json

{
  "subject": "Тема листа",
  "body": "Текст листа"
}
```

#### Ответ

```json
{
  "success": true,
  "data": {
    "message": "Email sent successfully",
    "sessionId": "session-123",
    "email": "user@example.com",
    "subject": "Тема листа"
  }
}
```

#### Ошибки

```json
{
  "success": false,
  "error": {
    "message": "No email available for this session",
    "code": "VALIDATION_ERROR"
  }
}
```

## Логирование

Все отправленные email логируются в таблицу `actions` с типом `EMAIL_SENT_ADMIN`:

```json
{
  "type": "EMAIL_SENT_ADMIN",
  "ref": "session-123",
  "sessionId": "uuid",
  "payload": {
    "email": "user@example.com",
    "subject": "Тема листа",
    "adminId": "admin-uuid",
    "adminEmail": "admin@example.com"
  }
}
```

## Безопасность

1. **Аутентификация**: Все запросы требуют JWT токен
2. **Валидация**: Все входные данные валидируются через Zod
3. **Логирование**: Все действия логируются для аудита
4. **Ограничения**: Максимальная длина темы - 200 символов, тела - 10000 символов

## Troubleshooting

### Email не отправляются

1. **Проверьте SMTP настройки**:

   ```bash
   # Проверьте переменные окружения
   echo $SMTP_HOST
   echo $SMTP_USER
   ```

2. **Проверьте логи**:

   ```bash
   # В логах backend должны быть сообщения об отправке email
   # Или ошибки, если что-то пошло не так
   ```

3. **Проверьте подключение к SMTP серверу**:
   ```bash
   # Используйте telnet для проверки подключения
   telnet smtp.gmail.com 587
   ```

### Ошибка "No email available for this session"

Эта ошибка возникает, если у сессии нет email адреса. Убедитесь, что:

- Пользователь предоставил email через Telegram бота
- Или платеж был обработан через PayPal и email был извлечен

### Email попадают в спам

1. **Настройте SPF записи** для вашего домена
2. **Настройте DKIM** подпись
3. **Настройте DMARC** политику
4. **Используйте правильный SMTP_FROM** адрес

## Примеры

### Отправка приветственного письма

```typescript
// В админ-панели через UI
// Тема: "Ласкаво просимо!"
// Текст: "Дякуємо за реєстрацію! Ми раді бачити вас серед наших користувачів."
```

### Отправка уведомления о платеже

```typescript
// Тема: "Підтвердження платежу"
// Текст: "Ваш платіж успішно оброблено. Дякуємо за покупку!"
```

## Дополнительные возможности

### Кастомизация шаблонов

Вы можете расширить `EmailService` для поддержки шаблонов:

```typescript
async sendEmailWithTemplate(
  email: string,
  template: string,
  variables: Record<string, string>
): Promise<void> {
  const html = this.renderTemplate(template, variables);
  await this.sendEmailToUser(email, variables.subject, html);
}
```

### Поддержка вложений

Для добавления поддержки вложений используйте `attachments` в nodemailer:

```typescript
await this.transporter.sendMail({
  // ... existing options
  attachments: [
    {
      filename: 'document.pdf',
      path: '/path/to/document.pdf',
    },
  ],
});
```

## Связанные файлы

- `backend/src/services/email-service.ts` - Email сервис
- `backend/src/controllers/admin-controller.ts` - Контроллер для отправки email
- `backend/src/validators/admin-validators.ts` - Валидация данных
- `frontend/src/components/SendEmailModal.tsx` - Модальное окно
- `frontend/src/pages/Payments.tsx` - Страница со списком платежей
- `frontend/src/services/api.ts` - API клиент

## Поддержка

Если у вас возникли проблемы с настройкой или использованием email функциональности, проверьте:

1. Логи backend приложения
2. Логи SMTP сервера
3. Таблицу `actions` в базе данных для истории отправки
