# 🔍 Настройка Sentry для мониторинга ошибок

Sentry интегрирован во все компоненты проекта (Backend, Frontend, Bot) для отслеживания ошибок в реальном времени.

---

## 📋 Что уже сделано

✅ Sentry интегрирован в:

- **Backend API** - отслеживание ошибок сервера
- **Frontend** - отслеживание ошибок React и JavaScript
- **Telegram Bot** - отслеживание ошибок бота

✅ Реализовано:

- Автоматическая отправка ошибок в Sentry
- Фильтрация чувствительных данных (пароли, токены)
- Контекст пользователя для лучшей диагностики
- Performance monitoring (10% в production, 100% в development)
- Session Replay для frontend (10% сессий, 100% ошибок)

---

## 🚀 Быстрый старт

### 1. Создание проекта в Sentry

1. Перейдите на [sentry.io](https://sentry.io)
2. Создайте аккаунт или войдите
3. Создайте новый проект:
   - **Backend**: Выберите "Node.js" → "Express"
   - **Frontend**: Выберите "React"
   - **Bot**: Выберите "Node.js" → "Express" (или можно использовать тот же проект что и Backend)

4. Скопируйте **DSN** (Data Source Name) для каждого проекта

### 2. Настройка переменных окружения

#### Backend

Добавьте в `backend/.env`:

```env
SENTRY_DSN=https://your-dsn@sentry.io/project-id
NODE_ENV=production  # или development
```

#### Frontend

Добавьте в `frontend/.env` или `frontend/.env.production`:

```env
VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id
```

**Важно:** Для Vite переменные окружения должны начинаться с `VITE_`

#### Bot

Добавьте в `bot/.env`:

```env
SENTRY_DSN=https://your-dsn@sentry.io/project-id
NODE_ENV=production  # или development
```

### 3. Настройка в Railway (Production)

Для каждого сервиса в Railway:

1. Откройте Settings → Variables
2. Добавьте переменные окружения:
   - **Backend**: `SENTRY_DSN`, `NODE_ENV=production`
   - **Frontend**: `VITE_SENTRY_DSN`
   - **Bot**: `SENTRY_DSN`, `NODE_ENV=production`

3. Сохраните и сделайте Redeploy

---

## 🔧 Конфигурация

### Backend (`backend/src/utils/sentry.ts`)

- **Traces Sample Rate**: 10% в production, 100% в development
- **Profiles Sample Rate**: 10% в production, 100% в development
- **Session Replay**: 10% сессий, 100% ошибок
- **Фильтрация**: Удаляет пароли, токены, секреты из событий

### Frontend (`frontend/src/utils/sentry.ts`)

- **Traces Sample Rate**: 10% в production, 100% в development
- **Session Replay**: 10% сессий, 100% ошибок
- **Masking**: Маскирует весь текст и медиа в replay
- **Фильтрация**: Игнорирует ошибки браузерных расширений

### Bot (`bot/src/utils/sentry.ts`)

- **Traces Sample Rate**: 10% в production, 100% в development
- **Контекст**: Включает chatId, sessionId, состояние бота

---

## 📊 Что отслеживается

### Backend

- ✅ Все неожиданные ошибки (5xx)
- ✅ Ошибки в middleware
- ✅ Ошибки в контроллерах и сервисах
- ✅ Контекст запроса (path, method, query)
- ✅ Информация о пользователе (если авторизован)

### Frontend

- ✅ Ошибки React компонентов (Error Boundary)
- ✅ JavaScript ошибки
- ✅ Ошибки API запросов
- ✅ Session Replay для воспроизведения ошибок
- ✅ Информация о пользователе

### Bot

- ✅ Ошибки обработчиков команд
- ✅ Ошибки обработки сообщений
- ✅ Ошибки API запросов
- ✅ Контекст чата и сессии
- ✅ Информация о пользователе Telegram

---

## 🛡️ Безопасность

Sentry автоматически фильтрует чувствительные данные:

- ❌ Пароли
- ❌ Токены (JWT, API keys)
- ❌ Секреты
- ❌ Authorization headers
- ❌ Cookies

Все данные фильтруются в `beforeSend` хуке перед отправкой в Sentry.

---

## 🧪 Тестирование

### Проверка работы Sentry

1. **Backend**: Создайте тестовый endpoint с ошибкой:

```typescript
app.get('/test-error', () => {
  throw new Error('Test Sentry error');
});
```

2. **Frontend**: Добавьте кнопку для тестирования:

```typescript
<button onClick={() => { throw new Error('Test Sentry error'); }}>
  Test Error
</button>
```

3. **Bot**: Отправьте команду, которая вызывает ошибку

4. Проверьте в Sentry Dashboard - ошибка должна появиться в течение нескольких секунд

---

## 📈 Мониторинг

### В Sentry Dashboard вы можете:

- 📊 Видеть все ошибки в реальном времени
- 🔍 Фильтровать по проекту, окружению, пользователю
- 📝 Видеть stack trace и контекст ошибки
- 👥 Видеть затронутых пользователей
- 📈 Отслеживать тренды ошибок
- 🎥 Смотреть Session Replay (для frontend)
- 🔔 Настраивать алерты (email, Slack, Telegram)

### Настройка алертов

1. В Sentry Dashboard → Alerts
2. Создайте новый Alert Rule
3. Настройте условия (например, >10 ошибок за 5 минут)
4. Добавьте уведомления (email, Slack, Telegram)

---

## 🔍 Отладка

### Если Sentry не работает:

1. **Проверьте DSN**: Убедитесь, что переменная окружения установлена правильно
2. **Проверьте логи**: Sentry выводит предупреждение если DSN не установлен
3. **Проверьте сеть**: Убедитесь, что сервер может отправлять запросы на sentry.io
4. **Проверьте окружение**: Убедитесь, что `NODE_ENV` установлен правильно

### Логи для проверки:

**Backend/Bot:**

```
Sentry initialized successfully { environment: 'production' }
```

**Frontend:**

```
Sentry initialized successfully { environment: 'production' }
```

Если видите предупреждение:

```
SENTRY_DSN is not set - Sentry error tracking is disabled
```

Значит переменная окружения не установлена.

---

## 📚 Дополнительные ресурсы

- [Sentry Documentation](https://docs.sentry.io/)
- [Sentry Node.js Guide](https://docs.sentry.io/platforms/javascript/guides/node/)
- [Sentry React Guide](https://docs.sentry.io/platforms/javascript/guides/react/)

---

## ✅ Чеклист настройки

- [ ] Создан проект в Sentry для Backend
- [ ] Создан проект в Sentry для Frontend
- [ ] Создан проект в Sentry для Bot (или используется общий с Backend)
- [ ] Добавлен `SENTRY_DSN` в `backend/.env`
- [ ] Добавлен `VITE_SENTRY_DSN` в `frontend/.env`
- [ ] Добавлен `SENTRY_DSN` в `bot/.env`
- [ ] Переменные окружения добавлены в Railway
- [ ] Протестирована отправка ошибок
- [ ] Настроены алерты (опционально)

---

**Готово!** Теперь все ошибки будут автоматически отправляться в Sentry для мониторинга в реальном времени. 🎉

