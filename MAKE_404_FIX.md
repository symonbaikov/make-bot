# 🔧 Исправление: Make Webhook 404 Not Found

## Проблема

Backend отправляет данные в Make, но получает ошибку `404 Not Found`:

```
❌ Failed to send webhook to Make {
  "error": "Request failed with status code 404",
  "status": 404,
  "statusText": "Not Found",
  "responseData": "Not found"
}
```

## Причины

1. **Неправильный URL webhook** - URL изменился или был скопирован неправильно
2. **Make сценарий не опубликован** - Webhook не активен, пока сценарий не опубликован
3. **Webhook URL устарел** - URL был изменен в Make после публикации
4. **Неправильный регион Make** - URL может быть для другого региона (us1, eu1, etc.)

## Решение

### Шаг 1: Проверьте текущий URL

В Railway или локально проверьте переменную окружения:

```bash
# В Railway
railway variables --service backend

# Или в логах backend при старте должно быть:
# "Make webhook URL configured" { url: "https://hook.us1.make.com/****" }
```

### Шаг 2: Получите правильный URL из Make

1. **Откройте Make сценарий:**
   - Войдите в https://www.make.com
   - Откройте ваш сценарий с webhook модулем

2. **Откройте Webhook модуль:**
   - Кликните на модуль "Webhooks" (розовый круг)
   - Откройте настройки модуля

3. **Скопируйте Webhook URL:**
   - В настройках модуля найдите поле "Webhook URL"
   - Скопируйте полный URL (например: `https://hook.us1.make.com/xxxxx/xxxxx`)

4. **Проверьте, что сценарий опубликован:**
   - В правом верхнем углу должна быть кнопка "Run once" или "Schedule"
   - Если сценарий не опубликован, webhook не будет работать

### Шаг 3: Обновите переменную окружения

#### В Railway:

1. Откройте Railway проект
2. Выберите сервис **backend**
3. Перейдите в **Variables**
4. Найдите `MAKE_WEBHOOK_URL`
5. Обновите значение на новый URL из Make
6. Сохраните изменения
7. Backend автоматически перезапустится

#### Локально:

Обновите `backend/.env`:

```env
MAKE_WEBHOOK_URL="https://hook.us1.make.com/your-actual-webhook-url"
```

Перезапустите backend:

```bash
cd backend
npm run dev
```

### Шаг 4: Проверьте, что сценарий опубликован

В Make:

1. Убедитесь, что сценарий **опубликован** (не в режиме редактирования)
2. Проверьте статус сценария - должен быть "Active" или "Scheduled"
3. Если сценарий не опубликован:
   - Нажмите "Run once" для теста
   - Или настройте расписание для автоматического запуска

### Шаг 5: Протестируйте webhook

#### Вариант 1: Тест через curl

```bash
curl -X POST https://hook.us1.make.com/your-webhook-url \
  -H "Content-Type: application/json" \
  -d '{
    "event": "bot_email_collected",
    "sessionId": "test-001",
    "email": "test@example.com",
    "tgUserId": "123456789",
    "firstName": "Test",
    "lastName": "User",
    "phoneNumber": "+1234567890",
    "plan": "STANDARD",
    "amount": 99.99
  }'
```

**Ожидаемый результат:**
- HTTP 200 OK (или 202 Accepted)
- В Make должно появиться новое событие с данными

**Если все еще 404:**
- Проверьте, что URL скопирован полностью (без пробелов)
- Проверьте, что сценарий опубликован
- Попробуйте создать новый webhook модуль в Make

#### Вариант 2: Тест через бота

1. Отправьте `/start` боту
2. Заполните данные (email, имя, телефон)
3. Проверьте логи backend - должно быть `✅ Webhook sent to Make successfully`
4. Проверьте Make - должно появиться новое событие

### Шаг 6: Проверьте логи после исправления

После обновления URL, при следующем webhook запросе должно быть:

```
✅ Webhook sent to Make successfully {
  sessionId: '...',
  responseStatus: 200
}
```

Вместо:

```
❌ Failed to send webhook to Make {
  status: 404
}
```

## Частые ошибки

### Ошибка 1: URL с лишними пробелами

```env
# ❌ Неправильно
MAKE_WEBHOOK_URL=" https://hook.us1.make.com/xxxxx "

# ✅ Правильно
MAKE_WEBHOOK_URL="https://hook.us1.make.com/xxxxx"
```

### Ошибка 2: Сценарий не опубликован

- Webhook не работает, если сценарий только в режиме редактирования
- Опубликуйте сценарий или запустите "Run once"

### Ошибка 3: Неправильный регион

- URL должен соответствовать региону вашего Make аккаунта
- Проверьте в настройках Make, какой регион используется
- Обычно: `us1`, `eu1`, `ap1`

### Ошибка 4: Webhook модуль удален/пересоздан

- Если вы пересоздали webhook модуль в Make, URL изменился
- Обновите `MAKE_WEBHOOK_URL` на новый URL

## Проверка после исправления

После обновления URL и публикации сценария:

1. ✅ Backend получает webhook от бота
2. ✅ Backend отправляет данные в Make
3. ✅ Make возвращает 200 OK (не 404)
4. ✅ В Make появляется новое событие с данными
5. ✅ Bundle содержит все поля из payload

## Дополнительная диагностика

Если проблема не решена:

1. **Проверьте логи backend:**
   ```bash
   # В Railway
   railway logs --service backend
   ```

2. **Проверьте логи Make:**
   - В Make откройте сценарий
   - Перейдите в "Execution history"
   - Проверьте последние выполнения

3. **Проверьте настройки webhook модуля:**
   - Убедитесь, что "Custom webhook" выбран
   - Проверьте настройки "Bundle content"

4. **Создайте новый webhook модуль:**
   - Если старый не работает, создайте новый
   - Скопируйте новый URL
   - Обновите `MAKE_WEBHOOK_URL`

## Контакты

Если проблема не решена после всех шагов:
- Проверьте, что Make сценарий активен и опубликован
- Убедитесь, что URL скопирован полностью без пробелов
- Попробуйте создать новый webhook модуль в Make
- Проверьте регион Make (us1, eu1, ap1)

