# ✅ Следующие шаги после применения миграций

## Что уже сделано:

✅ Миграции применены - таблицы созданы  
✅ Seed выполнен - админ создан (`admin@example.com` / `admin123`)  
✅ База данных готова к работе

## Проверка работы приложения:

### 1. Проверьте таблицы в Railway

1. Откройте Railway → PostgreSQL сервис → **Database** → **Data**
2. Должны быть таблицы:
   - `web_users` - с записью `admin@example.com`
   - `sessions` - с тестовой сессией `test-session-001`
   - `actions` - с тестовым действием
   - `users` - может быть пустая

### 2. Проверьте Backend сервис

1. Откройте Railway → Backend сервис (`@make-bot/backend`)
2. Убедитесь, что статус **Active** (не Crashed)
3. Если Crashed - проверьте Deploy Logs на ошибки

### 3. Попробуйте залогиниться

1. Откройте `https://make-botbackend-production.up.railway.app/`
2. Введите:
   - Email: `admin@example.com`
   - Password: `admin123`
3. Должен открыться Dashboard

### 4. Проверьте Health Check

```bash
curl https://make-botbackend-production.up.railway.app/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

## Если Backend все еще падает:

### Проверьте Deploy Logs

1. Откройте Backend сервис → **Deploy Logs**
2. Ищите ошибки:
   - `web_users does not exist` - миграции не применились
   - `DATABASE_URL` ошибки - неправильный URL
   - Другие ошибки - проверьте логи

### Пересоберите Backend

Если нужно пересобрать:

1. Откройте Backend сервис → **Deploy** → **Redeploy**
2. Проверьте Deploy Logs на наличие:
   - `Database ready!`
   - `✅ Database schema applied successfully!`
   - `✅ Server running on port 8080`

## Настройка переменных окружения (если еще не настроено):

### Обязательные переменные:

1. **Backend сервис** → **Variables**:
   - ✅ `DATABASE_URL` - уже установлен
   - ✅ `NODE_ENV=production` - должно быть установлено
   - ⚠️ `JWT_SECRET` - должен быть установлен (сгенерируйте случайную строку)
   - ⚠️ `CORS_ORIGIN` - опционально (например, `https://your-domain.com`)

2. **Bot сервис** → **Variables**:
   - ⚠️ `TELEGRAM_BOT_TOKEN` - токен от @BotFather
   - ⚠️ `API_URL` - URL Backend сервиса
   - ⚠️ `DATABASE_URL` - скопируйте из PostgreSQL сервиса

## Генерация JWT_SECRET:

Если `JWT_SECRET` не установлен:

```bash
# Сгенерируйте случайный секрет
openssl rand -hex 32
```

Или используйте онлайн генератор. Затем установите в Railway → Backend → Variables.

## Что дальше:

1. ✅ **База данных настроена** - миграции применены, seed выполнен
2. ⏭️ **Проверьте приложение** - попробуйте залогиниться
3. ⏭️ **Настройте переменные** - JWT_SECRET, TELEGRAM_BOT_TOKEN и т.д.
4. ⏭️ **Настройте Make сценарии** - для автоматизации (опционально)
5. ⏭️ **Настройте домены** - если нужны кастомные домены

## Troubleshooting:

### Не могу залогиниться

**Проверьте:**
- Backend сервис запущен (не Crashed)
- Логи Backend не содержат ошибок
- Таблица `web_users` существует и содержит админа

**Решение:**
- Проверьте Deploy Logs Backend
- Убедитесь, что seed выполнился успешно
- Попробуйте пересобрать Backend

### Backend падает при старте

**Проверьте:**
- `DATABASE_URL` установлен и правильный
- `JWT_SECRET` установлен
- Логи на наличие ошибок

**Решение:**
- Проверьте все переменные окружения
- Пересоберите Backend
- Проверьте Deploy Logs

### Таблицы не созданы

**Решение:**
- Примените миграции вручную (как делали ранее)
- Проверьте, что `DATABASE_URL` правильный
- Убедитесь, что Prisma schema существует

## Готово! 🎉

Если все проверки прошли успешно:
- ✅ База данных настроена
- ✅ Админ создан
- ✅ Backend работает
- ✅ Можно залогиниться

Теперь можно использовать админ-панель для управления приложением!

